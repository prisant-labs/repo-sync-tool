//! ipc - owned by E-06 (IPC payload structs shared with the Tauri shell).
//
// TODO(E-06): implement the IPC payload types (RepoSummary, RepoDetail,
// CheckResult, ...) with serde + specta::Type derives.
