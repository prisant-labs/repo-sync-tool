{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the RepoSync main window. E-01 skeleton: core defaults only. Later efforts add command/plugin permissions here.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default", "autostart:default"]
}
